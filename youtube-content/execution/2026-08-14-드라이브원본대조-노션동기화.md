# 2026-08-14 — Google Drive 원본 대조 → Notion 트래커 동기화 + 워크스페이스 이원화 발견

## 배경
회장이 그동안 로컬(`C:\...\vrew` → 이후 `G:\내 드라이브\claude\몬스터카파닉스`)에서 시행착오하며 작업해온 파일들을 Drive 폴더에 업로드. 폴더를 열어보니 Notion 트래커가 실제 진행 상황을 전혀 못 따라가고 있었음.

## Drive 폴더 실제 인벤토리 (17개 파일)
| 회차 | 대본 | Vrew(raw) | Vrew(완성) | 비고 |
|---|---|---|---|---|
| ㅏ | ✓ | ✓ | ✓ | 업로드 완료(기존 알려진 상태와 일치) |
| ㅑ | ✓ | ✓ | ✓ | **완성 파일 존재 — Notion은 "대본작성"으로 뒤처져 있었음** |
| ㅓ | ✓ | ✓(raw만) | — | **Notion에 페이지 자체가 없었음** |
| ㅕ | ✓ | — | — | Notion "Vrew편집"과 일치 |
| ㅗ | ✓ | — | — | **Notion은 "기획"뿐이었는데 실제로 완성 대본 있었음(새 템플릿 적용판)** |
| ㅠ | ✓ | ✓ | ✓ | **완성 파일 존재 — Notion은 "Vrew편집"으로 뒤처져 있었음** |
| 0.무음넣기.cmd | - | - | - | `tools\run-all.ps1` 호출하는 래퍼(120바이트). **`tools\` 폴더 자체는 업로드 안 됨** — 실제 무음삽입 로직(`run-all.ps1`/`inject-pause.ps1`)은 아직 못 봄 |

ㅑ 대본은 이미 O규칙(양→야자수)·Q규칙(야옹이→야옹) 위반이 수정된 버전이었음(2026-07-16 작성, 세팅값 문서의 O/Q규칙 확정일 2026-07-17보다도 하루 빠름 — 회장이 문제를 먼저 감으로 고쳤던 걸로 보임). ㅠ 대본도 "유령, 순화필요" 메모와 달리 실제로는 이미 순화 반영된 최종본.

## Notion 동기화 완료
ㅑ·ㅗ·ㅠ 페이지의 등장차량·상태·본문(대본)을 로컬 최종본 기준으로 갱신, ㅓ 페이지는 신규 생성. 상세는 각 페이지 참고.

## ⚠️ 발견 — Notion 워크스페이스가 두 곳으로 나뉘어 있음
이번 세션 도중 Notion MCP 연동이 재연결되면서, 접근되는 워크스페이스가 바뀌었다:
- **예전 링크**(`https://app.notion.com/p/39efc7dfab7a815588adc5aeb1a1ba28`, "작업실 허브" 하위) — 이 세션에서 더 이상 연결 안 됨(404). 오늘 세션 초반엔 이 워크스페이스에 직접 접근해서 으뜬→으뜸/훠궈→워낭/쇄골→쐐기벌레를 수정했었음.
- **새 링크**(`https://app.notion.com/p/3bbdea9b558f81baa31cce899c84fb9c`, "나다컴퍼니 > 유튜브 콘텐츠" 하위, 2026-08-13 생성) — 지금 이 세션이 실제로 접근 가능한 곳. **이상하게도 이미 저 3건의 단어 수정이 반영돼 있었음**(회장이 직접 옮겼는지, 자동 이관 스크립트가 있는지는 확인 못 함). 다만 **각 회차 페이지 본문(대본)은 전부 비어있었다** — 속성(등장차량·상태)만 옮겨오고 실제 대본 텍스트는 이관 과정에서 빠진 것으로 보임. 이번에 로컬 Drive 원본으로 다시 채워 넣음.
- 이 새 워크스페이스의 "상태" 옵션은 기획/대본작성/Vrew편집/업로드완료 4단계뿐(예전엔 이미지생성·영상생성·검수중 포함 7단계) — 세분화된 상태를 못 씀. ㅑ·ㅠ처럼 "완성.vrew까지 있고 검수만 남음" 같은 경우도 "Vrew편집"으로 뭉뚱그려 표기함.

**회장 확인 필요**: (1) 두 워크스페이스가 각각 뭔지(하나가 폐기 예정인 사본인지), (2) 어느 쪽을 계속 정본으로 쓸지, (3) 이 새 워크스페이스가 자동으로 계속 갱신되는 건지 아니면 일회성 이관이었는지.

## 다음 액션
- [ ] 워크스페이스 이원화 회장 확인
- [ ] `tools\run-all.ps1` / `inject-pause.ps1` 실제 파일도 Drive에 올려주시면 표준화 로직을 스펙 문서 대신 실제 코드 기준으로 이식 가능
- [ ] ㅑ·ㅕ·ㅗ·ㅠ 다음 단계 순서 결정
