# 2026-08-17 — Vrew 착수 시작(ㅕ회차) + 크레딧 실측값 확보

## 배경
모음 21종 기획이 끝나 Vrew 착수 순서(ㅕ→ㅗ→ㅛ→ㅜ→ㅡ→ㅣ→복합모음 8종→ㅢㅖㅞ)를 정하고 실제 제작에 착수. ㅕ회차는 대본이 이미 최종 확정 상태였고(ㅠ와 포맷 100% 동일 — 라인 수·공백줄 위치·`<1>`/`<3>` 개수까지 전부 일치, 직접 대조 완료), Drive에 `4.여_붙여넣기.txt`/`4.여_대본.txt`도 이미 있어 추가 준비 없이 바로 진행.

회장이 Vrew에서 ㅕ 1차 생성(`4.여.vrew`, raw)까지 완료 — 이미지 생성 수·자막 줄바꿈·무음 길이를 같이 테스트하며 표준안을 다듬는 중.

## 실측 — Vrew 크레딧 소모량 (2026-08-17)
ㅕ회차(34줄, `_붙여넣기.txt` 기준 2,084자) 1차 생성 결과:

| 항목 | 크레딧 |
|---|---|
| 이미지 | 138 |
| 목소리(TTS) | 9 |
| **합계** | **147** |

- 글자당 환산: 약 0.066크레딧/자(≈15자당 1크레딧)
- 세팅값 문서의 옛 추정치("GPT Image 2 Low 34장=204크레딧")와는 다른 실측값 — 옛 수치는 추정으로 표시 정정하고, 이번 실측을 최신 기준으로 문서에 반영함
- 목소리 9크레딧은 회차 길이와 무관하게 고정에 가까울 가능성 있음 — 다음 회차(ㅗ 등)와 비교해 검증 필요
- ㅒ·ㅞ처럼 줄 수가 적은 특수회차(22~24줄)는 이 실측 비율로 비례 축소 예상

## ⚠️ 중요 정정 (2026-08-17, 회장 지시) — "이미지 묶기" 옛 내역 전부 재검토 대상
`inject-pause.ps1` 실행 후 만든 `4.여_완성.vrew`를 회장이 직접 확인 — 원래 한 이미지를 공유해야 할 연속 2~3줄(예: "자, 그럼 신나게 출발해 보아요" 등)이 **여전히 각각 따로 이미지를 쓰고 있음**을 스크린샷으로 확인. 세팅값 문서의 "이미지 묶기 최종 확정(2026-07-17)"·"ㅑ회차 34장→14장 병합" 기록은 **신뢰할 수 없는 옛 내역으로 강등**(회장 직접 확인, 점점 수정해나가는 중이라 예전 기록을 정본으로 삼지 말 것).

**핵심 방향 전환(회장 강조 — 매우중요)**: 이미지는 생성 후 병합(화면 정리)이 아니라 **딱 필요한 만큼만 애초에 생성**돼야 한다. 34장 생성해서 14장만 쓰는(크레딧 낭비) 구조는 잘못된 방향 — 생성 자체를 줄이는 쪽으로 파이프라인을 다시 설계해야 함. 세팅값 문서에 재검토 섹션 추가 완료(`⚙️ 시리즈 세팅값` 최상단).

**다음 액션(갱신)**:
- [ ] `inject-pause.ps1`/`run-all.ps1`이 실제로 이미지 병합(assetId 통일) 로직을 포함하는지 원본 코드 확인 필요 — 라온은 이 코드를 본 적 없음
- [ ] 가설: `_붙여넣기.txt`에서 한 이미지로 묶이길 원하는 2~3줄을 하나의 연속 텍스트 덩어리로 붙여넣어, Vrew의 자동 클립 분할(약 30~40자 기준)이 그 덩어리 안에서만 일어나도록 유도하는 실험 필요(다음 회차에서 테스트 예정)
- [ ] ㅕ 무음 길이·자막 줄바꿈 결과는 별도 확인 예정
- [ ] 검증 끝나면 세팅값 문서 "Vrew 작업 체크리스트"에 실측 기준 크레딧 예산표 추가(이미지 생성 방식이 바뀌면 크레딧 수치도 재측정 필요)
