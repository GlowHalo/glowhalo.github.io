# Circle Heroes — 경쟁작 벤치마크 & 갭 분석

**작성 목적**: 아트 스타일이 다른 두 경쟁작(AFK Arena: Companions, HoC Legends)을 화면 단위로 뜯어본 결과, "디자인은 달라도 기본 시스템 구성은 거의 동일하다"는 게 확인됐다. 이 문서는 그 검증된 배치·타이밍·연출 문법 중 Circle Heroes가 **이미 갖고 있는 것 / 안 갖고 있어서 차용할 만한 것 / 의도적으로 다르게 갈 것**을 갈라서 정리하고, 남는 지점에서 킬러 콘텐츠 후보를 뽑는다.

**대상 독자**: PM/기획 세션, 디자인리소스(이미지) 세션, 게임코드 세션 — 전부 이 문서 하나로 같은 그림을 보고 시작하면 된다. `DESIGN.md`(확정안)를 뒤집는 문서가 아니라, 그 위에 "경쟁작 대비 우리 위치"를 얹는 문서다.

## 레퍼런스 자료

| 레퍼런스 | 특징 | 상세 자료 |
|---|---|---|
| **AFK Arena: Companions** | 2D 유화 일러스트, 원작 AFK 아레나의 스핀오프(오버월드 이동형). `DESIGN.md`가 이미 "지향 레퍼런스"로 명시한 계열 | [분석 리포트(아티팩트)](https://claude.ai/code/artifact/4152508c-2ba8-4ba6-a770-71e391fd5a54) — 원본 영상 GitHub Release 3개(`afkbm`=1부, `afkbm2`=2부, `afkarena3`=3부 클립 3개)를 ffmpeg 프레임 추출 후 분석한 20분 분량 리포트. 2026-07-29 원문 전체를 직접 읽고 §1~9 + R2(HoC 비교) 전 섹션 반영 완료 |
| **HoC Legends** | 실시간 3D 캐릭터 렌더링, 3D 다이오라마 허브타운 | 위 아티팩트 하단 "레퍼런스 2" 섹션에 통합 정리(스크린샷 7장 분석) |
| **마이티 아레나** | BlueStacks 실기 캡처, 3D 아이소메트릭 허브타운(주성) + 진영 필터(인간족/신선족/요괴족/신족/마족) | 주인님이 GitHub Release(`mighty_ref` 태그)로 업로드한 61초 실 게임플레이 영상(네이버 블로그 임베드 클립) — ffmpeg로 1초 간격 프레임 추출 후 주성/소환/연무장(아레나)/승급/육성/업적/모험/파티편성 전 화면 확인. 2026-07-29 §25~30으로 반영 |

**버튼 좌표·영웅 스프라이트 비율처럼 직접 픽셀을 재야 하는 작업은 `reference/` 폴더의 원본 스크린샷(1080×2340, 총 20장 — afk-arena-companions 14장 + hoc-legends 6장)을 열어서 확인할 것** — `reference/README.md`에 파일별 내용과 좌표 변환식 정리해둠.

두 게임은 아트 방향이 정반대(2D 회화 vs 3D 렌더링)인데도 **하단 탭 구성·가챠 구조·성장 축·전투 정보 표기 문법**이 거의 1:1로 겹친다. 이건 "우연히 비슷한 게 아니라 장르가 이미 정답에 수렴했다"는 뜻으로 읽는 게 맞다 — 즉 이 구성 자체는 자유롭게 차용해도 표절 리스크보다 학습비용 절감 효과가 훨씬 크다.

### AFK Arena: Companions 실측 수치(원문 §3, 1080×2340 캔버스 기준)

외부 아티팩트 링크가 언젠가 만료될 수 있어 핵심 수치만 이 저장소에도 복제해둔다. 420×740 논리 해상도로 환산하려면 `px / 1080 * 420`(x), `px / 2340 * 740`(y).

| 리소스 | 추정 크기(px) | 비율 | 배치 규칙 |
|---|---|---|---|
| 하단 탭 아이콘 | ~120×120 | 1:1 | 5등분 균등폭, 중앙(캠페인) 탭만 1.2배 확대+상단 돌출 |
| 우측 플로팅 아이콘 | ~85×85 | 1:1 | 세로 정렬, 간격 ~40px |
| 전투 스킬 속성 아이콘 | ~95×95 | 1:1 | 9개 가로 스크롤, 간격 ~8px |
| 파티원 포트레이트 카드 | ~165×230 | 약 5:7 | 좌상단 lv뱃지, 우하단 대여표시뱃지 |
| 참전 캐릭터 초상 카드(전투중) | ~165×290 | 약 4:7 | 초상+이름+HP바 세로스택, 5슬롯 균등폭 |
| 필드 위 캐릭터 스프라이트 | 세로 180–260 | 가변 | 전장 세로높이의 약 8~11%, **뒷줄일수록 살짝 축소**(원근감) |
| 상단 재화 아이콘 | ~44×44 | 1:1 | 숫자 좌측 부착, 우측 + 버튼(28px) |
| 영웅 상세 장비 슬롯 | ~145×145 | 1:1 | 좌우 3슬롯씩, 간격 ~30px |
| 도감/영웅목록 태그카드 | ~155×420 | 약 1:2.7(세로 긴 깃발형) | 4열 그리드, 하단 뾰족한 깃발+메달리온(직업아이콘) ~50×50 |
| 인벤토리 아이템 아이콘 | ~150×150 | 1:1 | 4열 그리드, 우하단 수량 |

## 대조 매트릭스 — Circle Heroes는 지금 어디에 있나

기호: ✅ 이미 구현(코드 확인함) · 🔶 부분 구현/확인 필요 · ⭕ 미구현·차용 후보 · ✋ 의도적으로 다르게 감(스코프 충돌)

| # | 검증된 패턴(레퍼런스 공통) | Circle Heroes 현황 | 근거 |
|---|---|---|---|
| 1 | 하단 5탭 + 우상단 플로팅(이벤트/우편/설정) | ✅ | `DESIGN.md` "메뉴 구조 Rev.C" |
| 2 | 소환 결과: 등급별 발광 차등 + 순차/일괄 공개 | ✅ | PROMPTS.md 진행기록(카드 뒷면·NEW 배지·재소환 버튼까지) |
| 3 | 소환 천장 카운터 상시 노출 | ✅ | `screens.ts:414` "천장까지 N회" — HoC의 "13/625"보다 오히려 더 직관적(잔여 횟수 직접 표시) |
| 4 | 데미지 숫자 임팩트 차등(일반<크리티컬) | ✅ | 진행기록 "크리티컬 시 랜덤 회전 펀치 + 카메라 흔들림" |
| 5 | 유닛 머리 위 실시간 HP바 | ✅ | `BattleScene.ts:404` (`hpBg`/`hpBar`, 캐릭터 상단 -11px) |
| 6 | 진영/속성 상성 구조 | ✅ | `DESIGN.md` 원소 순환+빛↔어둠 카운터 |
| 7 | 진영 시너지 편성 보상 표시 | ✅ | `screens.ts:221` 모노/레인보우 텍스트 표시 |
| 8 | 승급(각성) 전용 화면 + 재료 슬롯 | ✅ | 진행기록 "마이티아레나식 승급 화면(renderAscend)" |
| 9 | **승급 전/후 스탯 수치 미리보기**("공격/HP +40%/55%") | ⭕ | `renderAscend` 코드 확인 결과 수치 프리뷰 없음 — HoC에만 있던 패턴 |
| 10 | 영웅 상세에 **상성 관계(약점/강점) 안내 UI** | ⭕ | 코드 검색 결과 "상성" 표시 UI 없음(시너지 텍스트와는 별개) — AFK Arena Companions 패턴 |
| 11 | 필살기/궁극기 전용 풀스크린 컷인 연출 | ⭕ | 기존 이펙트는 히트 단위(E1~E8)까지만, 화면 전체를 장악하는 상위 연출 없음 |
| 12 | 캐릭터 감상/포토모드 | 🔶 | 전용 포토모드는 없지만, `assets/cards-webp/` 전신 일러스트는 2026-07-28에 연결 완료 — 영웅 상세화면 얼굴 썸네일 클릭 시 풀스크린 라이트박스로 확인 가능(`openIllustration`, `screens.ts`). 카드 아트가 없는 히든 5종은 자동으로 힌트 배지가 안 붙어 조용히 숨겨짐 |
| 13 | 레벨업 일괄 처리("N레벨 상승") | ✅ | 2026-07-29 원문 리포트 재확인: 이 패턴은 **HoC Legends 전용**("N 레벨 상승" 일괄, 비교표에 명시)이고 AFK Arena: Companions는 우리와 똑같이 **1레벨씩 개별 처리**. `save.ts` `tryLevelUp()`도 1레벨씩 — 두 레퍼런스 중 하나와 이미 일치, DESIGN.md 미니멀 지향과도 부합해 갭 아님 |
| 17 | 필드 위 캐릭터 스프라이트, 뒷줄일수록 살짝 축소(원근감) | ✅ | 2026-07-29 구현 — `BattleScene.ts` `HERO_ROW_SCALE`(뒷열 0.88배), 원문 §3 실측치("뒷줄일수록 살짝 축소") 반영 |
| 18 | 상단 고정바에 전투력 상시 노출 | ✅ | 2026-07-29 구현 — `#hud-power` 칩 추가(플레이어 레벨 개념은 DESIGN.md에 없어 제외, 파티 전투력만 표시) |
| 19 | 전체우편: 일괄 수령 버튼 | ✅ | 2026-07-29 구현 — `renderMailList()`에 "전체 수령 (N)" 버튼. 일괄 **삭제**는 우리 메일에 만료 개념이 없어 대응 기능 없음(스코프 아님) |
| 20 | 도감/영웅목록 태그카드 실측 비율(~155×420px, 약 1:2.7 세로 긴 깃발형) | ✋ | 2026-07-29 최초엔 `aspect-ratio: 2/4.6`로 근사했으나(1차), 주인님이 화면마다 카드 크기가 다른 게 위화감이 크다며 편성/보유 화면과 통일해달라고 하셔서 되돌림(2차) — 지금은 전 화면 공통 3:4 비율로 확정. 배너형 세로비율은 의도적으로 차용 안 함 |
| 21 | 퀘스트: 일일/주간/메인 + **상단 마일스톤 보상 트랙**(누적 포인트 20·40·60·80·100 구간마다 상자 자동 해금) | ✅ | 2026-07-29 구현 — 일일 임무 5개×20점=100점으로 설계해 임무 수령마다 포인트가 쌓이고 20/40/60/80/100 구간마다 별도 상자 수령(`missions.ts` `dailyPoints`/`milestoneClaim`, `renderMilestoneTrack`). 개별 임무 보상·전체완료 보너스와 별개인 3번째 보상 레이어로 완성 |
| 14 | 등각/3D 허브타운(건물별 콘텐츠 진입점) | ✋ | 2026-07-28 PM 결정으로 1차 구현했으나(2D 클릭형, `renderHub`) 2026-07-29 실기 테스트 후 "내가 원한 방식이 아니야"로 실사용 제거(§80, 코드는 히스토리 보존). 다만 그 "지붕색+아이콘+라벨" 오두막(hut) 타일 문법 자체는 폐기되지 않고 요일던전 5선택 화면(§24)에 그대로 재활용됨 — 목적지 선택 UI로서는 여전히 유효한 패턴이었던 것으로 판명 |
| 15 | 오버월드 이동형 스테이지 진행(맵 위를 캐릭터가 걸어감) | ✋ | `DESIGN.md` "무한 스테이지 파밍" 확정 — AFK Companions식 이동형이 아니라 원작 AFK 아레나식 스테이지 반복형에 더 가까움. 바꿀 이유 없음, 그대로 유지 권장 |
| 16 | 스킨(코스메틱) 탭 | ✋ | 스코프 밖(스킬트리도 없음 = 미니멀 지향). 낮은 우선순위 아이디어로만 보류 |
| 22 | 영웅 상세 장비 슬롯(AFK Arena Companions 실측, ~145×145 1:1 좌우 3슬롯) | ✅ | 2026-07-29 v2 재설계(3차) — 강화 레벨 방식(3슬롯, 1차) → 획득+장착 방식(5슬롯, 2차) → **레퍼런스와 동일한 6슬롯**(3차, 장신구를 목걸이=치명타 확률/반지=치명타 피해로 분리)까지 확장. 인스턴스 아이템(`EquipItem`)을 인벤토리에 모아 슬롯에 장착하는 문법(`renderEquipment`+`openEquipSlotModal`), 6슬롯×6스탯 1:1 매칭(무기=공격력·투구=체력·갑옷=방어력·신발=속도·목걸이=치명타 확률·반지=치명타 피해), 좁은 세로 캔버스에 맞춰 캐릭터 좌우 배치 대신 3열×2행 그리드로 재현. 시각적 슬롯 아이콘은 여전히 이모지 플레이스홀더 — 전용 아이콘 6종+등급별 아이템 아이콘은 ASSETS.md 백로그 |
| 23 | 무한의 탑/서브던전 구간별 몬스터 테마 순환 | ✅ | 2026-07-29 구현 — 스테이지 지역전환(§76)에서 만든 `STAGE_TIERS` 로스터를 재사용해 15층마다 순환(`TOWER_TIERS`). 신규 아트 없이 기존 스테이지 몬스터 키를 빌려써서 즉시 적용, 아트 도착 시 자동 활성 |
| 24 | 요일별 로테이션 던전(주간 보스) 선택 화면 | ✅ | 2026-07-29 구현 — 레퍼런스 20장 중엔 이 화면 유형이 없어 직접 벤치마킹 불가, 대신 §14에서 폐기했던 허브타운의 "지붕색 오두막" 타일을 재활용(`openRaidSelectModal`). 출전 규칙도 "오늘 보스와 같은 진영" → "보스를 상성으로 이기는 진영"으로 변경(`counterFactionOf`, `battle.ts`) — 이전엔 표시 전용이던 진영 상성표(§10)가 처음으로 실제 게임플레이 로직을 갖게 됨 |
| 25 | 최상단 네비게이션 골드→다이아→전투력 순서, 골드/다이아 클릭 시 상점 이동 | ✅ | 2026-07-29 구현 — `shell.ts` HUD 칩 순서 변경(`hud.append(gold, gems, power)`) + `.hud-chip.clickable` onclick으로 상점 탭 이동 |
| 26 | 소환화면 좌상단 확률고지 / 우상단 도움말(?) / 그 아래 원형 천장 진행률 배지 | ✅ | 2026-07-29 구현 — 레퍼런스는 이 자리에 "소환 포인트"(교환상점용 신규 재화)를 뒀지만, 우리는 신규 재화를 만들지 않고 기존 UR 천장(`save.pity`/`UR_PITY_LIMIT=200`)을 그 자리에 그대로 시각화(`.pity-ring`, conic-gradient). 도움말은 확률표와 별개로 소환 메커니즘을 말로 풀어 설명하는 신규 모달(`openSummonHelpModal`) |
| 27 | 등각/3D 허브타운 재해석 — "주성" 탭(건물 클릭지점 → 하위메뉴 이동) | ✋ | 2026-07-29 인프라만 구현 — §14/§80과 달리 3D는 채택 안 하고 기존 2D 클릭형 문법(§24와 동일 오두막 타일)을 그대로 재사용. 하위메뉴 내용은 사용자가 구조를 따로 정할 예정이라 지금은 건물 자리 6개(`FEATURE_GATES`)만 배치, 클릭하면 "준비 중" 토스트만 뜬다 |
| 28 | 스테이지 클리어로 여는 기능 게이트 + 해금 시 상단 배너 알림 | ✅ | 2026-07-29 구현 — `systems/featureGates.ts` 신규(`FEATURE_GATES`/`isFeatureUnlocked`/`checkNewlyUnlockedGates`), 해금 시 모달이 아니라 상단에서 내려왔다 올라가는 배너(`.gate-banner`). 이미 스테이지 1부터 열려 있던 기존 기능(장비/무한의탑/아레나/요일던전)은 소급 잠그지 않고, §27 주성 건물 자리에만 적용 |
| 29 | 연무장(아레나) 주간 마일스톤 보상 트랙 | ✅ | 2026-07-29 구현 — 레퍼런스의 아레나 랭킹 자체는 서버 매칭 전제라 스코프 밖으로 제외하고, "임무 완료마다 포인트가 쌓여 상자를 여는" 트랙 구조만 §21(일일)에서 그대로 떼어 주간 임무(`WEEKLY_MISSIONS`)에 재적용(`WEEKLY_MILESTONE_TRACK`, `renderMilestoneTrack` 공용화) |
| 30 | 오프라인 방치 보상 상한 시간 표기 | ✅ | 2026-07-29 구현 — 적립 방식(온라인 tick)은 그대로 유지, 방치 보상 모달에 "최대 240시간" 문구만 추가(`OFFLINE_CAP_HOURS`) |
| 31 | 등급별 카드/아이템 배경색 채움(테두리뿐 아니라 뒷 여백도 등급색) | ✅ | 2026-07-30 구현 — task #37에서 분리해뒀던 "카드=진영색/얼굴틀=중립색" 구조를 안 건드리고, 카드 바로 바깥에 등급색 매트(box-shadow 링+발광)를 두르는 "외곽 매트형"으로 구현(`setGradeBorder` 확장, `GRADE_GLOW` 전 등급 적용). PNG 프레임(등급×카드형태별 15장 이상 필요)으로 가는 안은 ASSETS.md 백로그로 분리 |
| 32 | 장비 강화 — 등급별 성능 위에 강화 레벨(최대 +10) 가산, 강화석 직접 투입 + 다른 장비 흡수(이월) | ✅ | 2026-07-30 신규 구현 — 장비 시스템 v2(§22, "강화 없이 등급 교체만")에서 다시 한 단계 확장. `EquipItem.invested/level` 필드 신설, `EQUIP_LEVEL_COST`(등급별 레벨당 강화석)·`EQUIP_ABSORB_BASE_VALUE`로 흡수 시 가치 환산. 이미 강화된 장비를 흡수하면 그 투입치까지 그대로 이월(확인 팝업 후) |
| 33 | 소환을 일반/고급/픽업 3갈래로 분리, 소환권(일반·고급) 소모, 픽업소환은 후보 3명 중 선택 | ✅ | 2026-07-30 신규 구현 — 레퍼런스의 "기본/우정/고급소환" 3탭 + "확률업 소환(영웅 선택형 픽업)" 문법을 우리 경제에 맞게 재해석. 일반소환은 N~SR만(SSR/UR 없음, `NORMAL_GRADE_WEIGHT`), 고급소환은 기존 전 등급표 그대로, 픽업소환은 매달 로테이션되는 후보 3명 중 1명을 화면 중앙에서 골라 그 영웅만 레이트업(`gacha.ts` `pickupCandidates`/`pickupPityCount`) — 픽업 천장은 영웅 id 기준으로 쌓여 후보를 바꿔도 이전 진행도가 사라지지 않음 |
| 34 | 영웅 승급 재료 재설계 — "동일 캐릭터 복제" 대신 "동일 성급 아무 영웅 2체"(4→5성만 진영도 일치) | ✅ | 2026-07-30 재설계 — 기존 지수적 복제 요구(1→5성 총 15장)를 폐지하고 단계당 고정 2장으로 단순화, 재료 풀도 "그 캐릭터 자체"에서 "동일 성급 전체"로 확장해 F2P 접근성 대폭 개선. 재료 선택 UI 신설(`renderMaterialPicker`) — 후보를 눌러 추가, 채워진 재료 슬롯을 눌러 제거 |
| 35 | 5성 이후 초월(보라색 별) 트랙 — 1단계는 동일 영웅 5성 완본 소모, 2~5단계는 1성 영웅 2장씩 | ✅ | 2026-07-30 신규 구현(레퍼런스에 없던 개념, 사용자 설계 그대로 반영) — `save.transcend`(0~5) 필드 신설. 승급 화면에서 5성 달성 시 자동으로 초월 UI로 전환(`tryTranscendStep1`/`tryTranscendStep`) |
| 36 | 영웅 레벨 초기화(환생 대신) — 레벨업에 쓴 골드 전액 환급 | ✅ | 2026-07-30 구현 — "환생"은 채택하지 않고 레벨(골드 투자분)만 초기화. 승급(성급) 재료는 추적이 안 돼 환급 대상에서 제외(사용자 확인받음). 위치는 임시로 영웅 상세화면에 배치(정확한 UX 배치는 추후 결정 예정) |
| 37 | 전투 탭을 4갈래 칩 대신 "스테이지"+"모험" 2갈래로, 요일던전/아레나/무한의탑은 별도 배너 허브로 이동 | ✅ | 2026-07-30 신규 구현(전달한 마인드맵 메뉴안 전체 승인) — 기존 오두막(hut) 타일 문법(§14/§24에서 이미 재활용된 패턴)을 세 번째로 재사용해 `openAdventureHubModal()` 신설. `TABS`의 battle `subs`를 `["스테이지","모험"]`로 축소, `BATTLE_MODES`도 대응 축소 |
| 38 | 소환 일일 무료 1회 — 매일 정오/자정 경계로 초기화, 일반·고급 각각 별도 카운트, 미사용시 소멸(비누적) | ✅ | 2026-07-30 신규 구현(사용자 설계: "소환권을 주는게 아니라 시간이 되면 1회소환버튼이 무료소환 버튼이 되고 빨간점찍어서 알려주는거야") — 소환권 지급이 아니라 "1회 소환" 버튼 자체가 "무료 소환" 버튼으로 상태 전이(`gacha.ts` `freeSummonAvailable`/`freeWindowKey`, 12시간 반구간 키 비교). 소환탭 버튼에 빨간 점 + 하단 탭바 소환 아이콘에도 배지(기존 우편함 배지 패턴을 탭바용으로 일반화) |
| 39 | 오프닝(스플래시) 화면 — 최초 실행/재접속 시 브랜딩 화면, 탭하면 진입 | ✅ | 2026-07-30 신규 구현 — 화려한 브랜딩 이미지는 아직 없어 기존 navy/gold 톤 그라디언트로 대체(`splash.ts`, 이미지 백로그는 ASSETS.md 등록). `main.ts`에서 Phaser 게임 초기화 뒤 `renderSplash(buildShell)`로 셸 빌드를 dismiss 콜백으로 미룸 |
| 40 | 소환 확률 재조정 — 일반소환에 초저확률 SSR 추가, 고급소환은 SR~UR만(N/R 제외), 픽업은 SSR 적중 시 100% 확정(더 이상 50/50 아님) | ✅ | 2026-07-30 재조정(사용자 지시: "소환관련 네가말한대로 진행", "픽업100%지만 고급소환에 차별점을 추가하지는 않음") — `NORMAL_GRADE_WEIGHT`에 SSR 0.1% 추가(N59.9/R35/SR5/SSR0.1), 프리미엄 등급표를 SR~UR만 남기고 N/R 제거. 픽업소환은 SSR 롤 시 무조건 지정 영웅 지급(레이트업이 아니라 확정 지급) — 고급소환 쪽엔 의도적으로 보상 차별점을 추가하지 않음(사용자가 명시적으로 요청) |
| 41 | 카드 표시방식 최종 정리 — 배경=등급색(C안), 테두리=공통 프레임(등급 구분 없음), 진영=좌상단 코너 배지, 성급+초월=별 하나로 통합 | ✅ | 2026-07-30 재정리(사용자와 논의 후 결정) — B안(외곽 매트형)에서 C안(전체 배경 채움)으로 전환(`setCardGrade`, `GRADE_CARD_BG`), 진영은 카드 전체 그라디언트 대신 좌상단 아이콘 배지로 이동(`addFactionBadge`, task #37의 "진영=카드배경" 구조를 대체). 테두리는 등급별 색·굵기·발광(`GRADE_GLOW`)을 걷어내고 각 컨테이너의 기본(공통) 프레임만 남김 — 장비 아이템은 카드 형태가 아니라서 기존 등급 매트 방식(`setGradeBorder`) 그대로 유지. 성급(★, 골드)과 초월(✪, 보라)은 "황금별6이 아니라 보라별1로" 설계대로 같은 5칸 별 자리를 공유해 초월 시작 시 통째로 색이 바뀌며 반짝이는 발광 애니메이션 추가(`.star-purple`, `prefers-reduced-motion` 대응) |
| 42 | 영웅 카드 그리드 4열 + 하단 별 한 줄(축약 없음) + UR 상시 반짝임 + 얼굴 여백에 등급색 비침 | ✅ | 2026-07-30 구현 — 그리드 5열→4열(`hero-grid`), 좌하단 압축 배지("★N")를 카드 하단 전체 폭 별 5개 줄로 교체(`hc-star-row`, `starRowText`), 우하단이던 클래스 배지는 우상단 Lv 배지 밑으로 이동해 별 줄과의 충돌 해소. UR 카드는 늘 은은하게 펄스 발광(`grade-shimmer-ur`, box-shadow 애니메이션이라 얼굴 이미지엔 영향 없음). `.face` 배경을 고정 검정(`#0a0d16`)에서 투명으로 바꿔, 누끼(배경 제거) PNG의 빈 여백에 부모 카드의 등급색 그라디언트가 그대로 비치도록 — 캐릭터와 카드 배경이 하나로 스며듦. 클래스 표시 방식 자체(현재는 이모지 코너 배지)는 사용자가 "고민하자"로 열어둬 이번엔 그대로 유지 |
| 43 | 소환/전투/모험 탭 구조 재편 — 확률 서브탭 제거, 메뉴 클릭 시 항상 대표화면으로, 주성 삭제+모험 독립 탭 승격 | ✅ | 2026-07-30 구현 — **소환**: 원래도 있으나마나였던(클릭하면 "준비 중" 토스트만 뜨던) "확률·천장" 가짜 서브탭 삭제. 실제 확률고지·천장 링은 이미 "영웅 소환" 단일 화면에 다 있었어서(§26) 코드는 서브탭 항목만 지우면 됐다. **탭 리셋**(item 7, "메뉴를 누르면 항상 대표화면으로"): `switchTab`에서 영웅/임무 탭 클릭 시 마지막 서브뷰 기억 대신 매번 기본값(보유·편성/일일)으로 리셋, 전투 탭 클릭 시 배틀모드를 강제로 스테이지로 되돌림. **주성 삭제**: 하위메뉴가 끝내 미정으로 남아있던 건물 자리표시자 6개짜리 껍데기 탭을 완전히 제거(`renderCastle`/`FEATURE_GATES`/게이트 배너 시스템 전부 삭제). **모험 독립 탭**: 전투 탭 서브메뉴였던 모험 허브(요일던전/아레나/무한의탑 배너 3개)를 전투 탭 오른쪽의 독립 메인 탭으로 승격(`renderAdventure`) — 탭 재진입 시 항상 픽커부터 다시 보여주고(item 7과 동일 원칙), 배너를 고르면 스테이지 탭과 똑같이 투명해져 Phaser 캔버스가 그대로 비침(`showAdventureCanvas`, `#screen-adventure.adventure-live`). 구현 중 "화면 컨테이너만 투명하게 하면 픽커 UI가 캔버스 위에 그대로 겹쳐 보인다"는 버그를 발견해 `innerHTML`까지 비우도록 수정 |
| 44 | 전 화면 정비 스윕 — 모든 탭·서브뷰·모달을 직접 눌러보며 버그 2건 발견·수정 | ✅ | 2026-07-30 "우선 모든 영역 하나씩 눌러보면서 체크해서 정비해줘" 요청으로 영웅(보유/장비/승급/도감/상세/레벨업/레벨초기화/편성해제/일러스트)·소환(3카테고리+도움말+확률고지+10연 뽑기 애니메이션 전체)·전투·모험(픽커/요일던전 모달/아레나/무한의탑)·상점(구매→장비 인벤토리→장착→강화)·임무(일일/주간/업적)·우편함·설정을 Playwright로 전수 클릭. 발견한 버그 2건: (1) 소환 도움말 팝업이 §40 확률 재조정 이전 문구("고급소환은 전 등급이 균등하게")를 그대로 갖고 있어 실제 동작(SR~UR만)과 어긋남 — 텍스트 갱신. (2) 요일던전에서 편성에 상성 진영 영웅이 없으면 `BattleScene.startRaid()`가 조용히 스테이지로 폴백하는데(기존 로직), 모험이 독립 탭으로 승격되면서 이 폴백이 일어나도 하단 탭바는 계속 "모험"을 가리키는 채로 남아 탭 표시와 실제 콘텐츠(스테이지)가 어긋나는 새 불일치가 생김 — `battle-mode-changed` 리스너에서 모험 탭에 있는 채로 스테이지로 폴백하면 전투 탭으로 같이 전환하도록 수정 |
| 45 | 카드 구조를 party-slot식(아이콘+이름) 세로 스택으로 전 화면 통일 + 장비 탭을 인벤토리 우선으로 재구성 | ✅ | 2026-07-30 구현 — "4열로 줄여달란 건 개수가 아니라 어느 화면이나 카드 모양을 통일해서 일관성+소스복잡도를 줄이고 싶었던 것, 편성화면(party-slot) 구조가 가장 마음에 든다"는 피드백에 맞춰 `.hero-card`를 얼굴이 카드를 거의 채우던 §41의 C안에서 party-slot과 동일한 "작은 정사각 아이콘(62%)+이름+별" 세로 스택으로 재작성. 이 카드 하나를 영웅 보유·도감·재료픽커·장비 인벤토리가 전부 공유(`buildHeroCard`/`buildEquipCard`). 장비 탭은 "영웅부터 고르는" 기존 화면을 폐기하고 보유 장비를 등급 내림차순으로 훑고 슬롯 필터(기존 진영칩 `.f-chip` 재사용)로 좁혀볼 수 있는 인벤토리 화면으로 재구성, 장착/해제는 영웅 상세화면의 신규 "✨ 장비" 섹션(6슬롯 그리드)으로 이동. 구현 중 발견한 버그 2건: (1) 재료픽커의 mat-count 배지가 카드 레이아웃 변경 전부터 Lv 배지와 같은 우상단 자리를 썼던 걸 발견해 좌하단으로 이동. (2) 영웅 상세 장비 섹션의 "장착" 버튼이 기존 로직 그대로 `closeModal()`을 호출해, 장비탭에서 열 때(화면 리렌더)는 문제없지만 영웅상세에서 열 때(모달이 모달을 감싸는 구조)는 장착 직후 모달 전체가 사라지는 버그로 이어짐 — 인접한 해제/판매 버튼처럼 self-refresh(`openEquipSlotModal` 재호출) 패턴으로 통일해 출시 전에 수정. Playwright로 영웅그리드/도감/재료픽커 카드 통일, 장비탭 인벤토리+필터+미장착 카드 클릭→장착할 영웅 고르기→장착 완료, 영웅상세 장비섹션에서 슬롯 클릭→해제(self-refresh로 모달 유지 확인)까지 전체 플로우 실동작 검증 |
| 48 | 마이티 아레나 전투 페이싱 실측 반영 + 아레나를 "상대 목록에서 도전 선택"으로 재설계 | ✅ | 2026-07-31 구현 — 사용자가 이전에 캡처해둔 마이티 아레나 실제 플레이 영상(`mighty_ref.mp4`, 68초)을 0.5초 간격으로 프레임 추출해 전투 시퀀스(도전 시작~승리까지 ~13.5초)를 분석: 스킬은 항상 "스킬명 배너 예고(~0.5~1초) → 이펙트 판정(~1~1.5초)"로 총 1.5~2.5초, 기본공격류 연속 타격은 ~0.5초 간격. 이 실측치를 반영해 (1) `castGlow()`에 스킬명 텍스트 배너(기존 `showBanner()` 재사용, 진영색 틴트)를 새로 추가 — 지금까진 오라링 이펙트만 있고 무슨 스킬을 썼는지 텍스트 정보가 없었음, (2) 턴제(탑/아레나) 행동 간 gap을 기본공격 380→450ms, 스킬 450→1100ms, UR필살기 830→1400ms로 재조정(기본공격은 스냅피하게 유지, 스킬 연출은 눈에 담을 시간 확보). 또한 "아레나 공격대상 지정"이 실은 전투 중 타겟팅이 아니라 도전 전 상대 파티를 랭킹 목록에서 고르는 것이었다는 정정 신고를 받아, 이전 라운드에 잘못 만든 전투-중-대상선택 모달(`promptArenaTarget`, `forceTargetKey` 전체 배선)을 완전히 되돌리고(전투는 항상 완전자동이라는 명시적 규칙 반영), 신규 `systems/arenaMatch.ts`로 이름·점수·전투력이 다른 상대 3명을 생성해 "연무장" 모달(마이티 아레나 레퍼런스 그대로)에서 "도전" 버튼으로 고르는 방식을 신설. 전투 종료 후엔 자동으로 다음 상대와 재대결하지 않고 새로 뽑은 목록으로 복귀(`arena-round-ended` 이벤트). Playwright로 목록 렌더링(이름/점수/전투력 3행)→도전 클릭→전투 시작→8초 후 자동으로 연무장 목록 복귀까지 전체 플로우 검증. 턴 순서(왼쪽 진영부터 시작 vs 기존 양팀 속도 통합 정렬) 리서치 결과를 공유하고 사용자에게 확인받은 결과 **"지금처럼 양팀 통합 속도큐 유지, 아레나·무한의 탑에만 적용(스테이지·요일던전은 실시간 그대로)"로 확정** — 즉 코드 변경 없음. 근거로 제시한 리서치: 기존 `stepTurn()`이 이미 Summoners War/RAID식 "양팀 통합 속도 큐"(턴제 가챠RPG 업계 표준)이고, 사용자가 제안했던 "진영별로 몰아서" 방식은 세븐나이츠 그랜드크로스식 팀페이즈로 다른 방향이었다는 점, 그리고 "슬롯1 탱커가 속도와 무관하게 먼저 움직이는지"는 두 방식 다 아니라는 점(속도가 슬롯보다 항상 먼저 비교됨)을 확인시켜드림 |
| 49 | 아레나 랭킹 사다리 시스템(5명 후보+순위 교체+주간 순위 보상) | ✅ | 2026-07-31 구현 — 사용자가 "일반적인 아레나유형을 분석해서 제안해달라"며 구체적 설계안(1000위 시작, 순위 부근 +1~20에서 4명·-1~5에서 1명 총 5명 후보, 승리 시 위쪽 상대와 순위 교체·패배나 아래쪽 상대 승리는 순위 유지, 주간 순위 보상)을 제시. 실제 매칭 서버 없는 싱글플레이 게임이라 Summoners War/RAID식 ELO 매칭보다 "챌린지 사다리"(순위표 위 칸을 이기면 자리를 바꿔치기) 문법이 시뮬레이션에 더 적합하다고 판단해 그대로 채택(레퍼런스: 전통적인 챌린지 래더 + 마이티 아레나 "랭킹 보상" 탭 구조). `save.arenaRating`(레이팅 증감식)을 `save.arenaRank`(1000 시작, 순위 자체가 상태값)로 전면 교체, `systems/arenaMatch.ts`에 rank를 시드로 쓰는 결정적 의사난수(mulberry32)로 "그 순위 자리에 항상 같은 더미 상대가 있다"는 일관성 구현, 후보 목록도 3명→5명(위 4+아래 1, 최상위권이라 위쪽 범위가 부족하면 아래쪽에서 자동 보충)으로 확장. 주간 순위 보상(`ARENA_WEEKLY_REWARDS`, 1위/2~10위/11~50위/51~200위/201~500위/501~1000위/1001위+ 7단계, 상위일수록 골드+확정 등급 장비)을 신설해 기존 주간임무와 동일한 ISO 주차 리셋(`isoWeek()`를 missions.ts에서 export해 재사용) 패턴으로 주 1회 수령. 부수 작업: `grantRandomEquip()`에 `forceGrade` 옵션 추가(등급 확정 지급용), 레이팅 기준이던 아레나 업적 4종(1200/1400/1600/1800)을 순위 기준(800/500/200/50위 이내 진입)으로 재설계 — 업적 시스템이 "높을수록 진행"만 가정하고 있어 "2000-순위" 파생 점수로 기존 higher-is-better 비교 로직을 그대로 재사용(순위 성격이 정반대인 걸 감안한 최소 침습 설계). Playwright로 강한 테스트 파티 vs 위쪽 상대 승리 시 순위가 정확히 그 상대 순위로 교체되는 것, 아래쪽(안전픽) 상대 승리 시 순위 불변인 것, 주간 보상 클레임 시 해당 구간 골드가 정확히 지급되는 것(201~500위 구간 700골드 실측 일치), 업적 화면에서 파생점수 진행바가 클램프까지 정상 표시되는 것까지 실동작 검증 |
| 47 | 14건 일괄 피드백 — 전투 연출/배속/모험 배너/상점 버그/UI 정비/파티편성/아레나 타겟팅 등 | ✅ | 2026-07-30 구현 — 배경 리서치 에이전트 2개(전투 계열/UI·버그 계열)를 먼저 병렬 조사시켜 원인을 확인한 뒤 코드 작업. **①UR 필살기 오발동 버그**: 마초(UR) 기준으로 3턴마다 무조건 풀스크린 컷인이 뜨던 원인은 `isUrCast`가 "UR 등급이면"만 보고 "이번 스킬이 실제로 컷인 받을 자격이 있는지"를 안 봤던 것 — 웨이브당 1회만 재생되도록 `ultimateCutinPlayedThisWave` Set 추가. **②배속 재조정**: "마이티아레나 2배속≈우리 1배속" 체감 보고에 맞춰 `speedMult` getter를 `speedTier*0.5`로 바꿔 기존 25곳+ 호출부를 안 건드리고 전체 페이싱을 절반으로 늦춤(버튼 라벨 x1/x2/x3는 유지). **③속도 스탯 질문**: 답변 — 실시간 페이싱(배속)과 턴 순서(민첩 기반)엔 이미 반영되지만, "느린 쪽이 두 번 맞는" 더블어택류 메커닉은 없음(질문이라 미구현 상태 유지, 필요하면 다음 라운드에). **④모험 배너 재설계**: 2열 오두막 그리드→가로 긴 배너 리스트(요일던전/아레나/무한의탑, 마이티아레나 참고)로 교체, 배너 아트 3종 ASSETS.md 백로그 등록(지금은 CSS 그라디언트+이모지). **⑤소환권 수량 버그**: 실제 보유 수량(ticketNormal/Premium)은 원래도 독립적이었고, 진짜 버그는 "오늘의 할인" 일일 구매 한도 카운터가 종류 구분 없는 단일 필드였던 것 — `save.ticketDiscountDate/Bought`를 kind별 `Record`로 분리(구버전 세이브 마이그레이션 포함). **⑥무료소환 빨간점 버그**: `freeAvailable`이 `const`라 렌더 시점에 한 번만 계산되고 소환 성공 후 재계산이 안 됐던 걸 `let`+콜백 내 재계산으로 수정. **⑦버튼 전수점검**: 마일스톤 보상상자가 `<div onclick>`이라 버튼 접근성이 없던 걸 `<button>`으로 교체(유일한 실제 코드 버그), 나머지 CTA는 이미 버튼 배경 아트가 적용돼 있어 ASSETS.md에 결론과 함께 정리. **⑧테스트 재화**: 라이브 브라우저 localStorage에 직접 쓸 방법이 없어 기존 시크릿 코드(GOLD/DIA) 지급량을 100만 골드/1만 다이아로 상향, 코드 입력으로 받는 방식 안내. **⑨진영 시너지 텍스트**: `renderHeroes`의 `partyFactions` 계산이 전투 로직(`buildTeam`)과 달리 "파티에 있지만 재료로 소모돼 owned=0인 유령 영웅"을 걸러내지 않던 비대칭을 발견해 통일 — 단, 이 비대칭은 수학적으로 신고 증상(시너지 있는데 "없음"으로 표시)과 반대 방향이라 재현 안 되면 추가 제보 필요. **⑩영웅화면 진영 아이콘화**: 진영칩 텍스트 라벨 제거하고 아이콘만(한 줄에 맞춤), "불명"(히든) 전용 ❓ 아이콘 추가. **⑪요일던전 파티편성 화면 신설**: 오두막 선택 후 바로 전투가 시작되던 걸 재사용 가능한 `openPartyFormationModal()`(기존 party-row+영웅그리드 패턴 재사용) 경유로 변경 — 편성 확인 후에만 전투 시작. **⑫서큐버스 방향**: 원본 아트가 창을 왼쪽으로 들고 있는 걸 육안 확인 후 `REVERSED_FACING_KEYS`에 추가. **⑬아레나 타겟팅**: "리스트에서 대상 선택"이 반영 안 됐다는 신고로 신규 구현 — `pickTarget`/`act`에 `forceTargetKey` 스레딩, 아레나 모드+일반공격 턴에서만 `promptArenaTarget()` 모달(적 목록+HP%)을 띄우고 고른 대상이 타운트보다 우선하도록. 스킬시전 턴은 각 영웅 고유 AI(AOE/힐 등)를 건드리지 않기 위해 이 강제타겟에서 제외. **⑭무한의탑 임프 방향**: 신고 그대로 `REVERSED_FACING_KEYS`에 추가(아트 자체는 육안상 모호해 사용자 신고를 신뢰). Playwright로 5명 동일진영 파티 시너지 텍스트, 모험 배너 3장 렌더링, 요일던전 진입→파티편성 모달→전투 시작까지 전체 플로우, 무료소환 사용 전/후 탭바 빨간점 소멸, 할인 티켓 5회 구매 후 다른 종류 수량 미차감까지 실동작 검증 |
| 46 | 영웅강화 누르고있으면 연속 진행 + 영웅상세/소환/상점/임무 화면 스크롤 없이 한 화면에 + 영웅 목록 성급>등급>진영 정렬 + 딜리버리된 이미지 반영 | ✅ | 2026-07-30 구현 — 5개 요청을 한 라운드로 처리. **①레벨업 누르고 있기**: pointerdown 즉시 1회+420ms 뒤부터 130ms 간격 자동 반복(`levelHoldTimer`/`levelHoldInterval`, 해제는 `window` pointerup/pointercancel). 레벨업마다 모달을 통째로 다시 그리는 기존 구조라 재호출마다 방어적으로 걸어뒀던 `stopLevelHold()`가 반복 중인 자기 자신의 인터벌을 즉시 꺼버리는 버그를 실제로 길게 눌러보는 Playwright 테스트로 발견·제거. **②스크롤 없이 한 화면**: `.modal` max-height 78vh→90vh, 스탯 5줄을 2열 그리드로, 장비 슬롯 박스 3:4→1:1 정사각(가장 큰 절감), 상점/임무 리스트 카드·마일스톤 박스 패딩 전반 압축. 조사 중 상점 서브탭(골드/보석/일일무료)이 실제로는 화면을 안 바꾸는 죽은 UI였던 걸 발견 — heroes/missions처럼 분기 처리가 안 돼 있어서 클릭하면 "준비 중" 토스트만 뜨던 것을 실제로 8개 항목을 3개 탭으로 나누도록 고쳐서 오버플로 문제와 버그를 동시에 해결. 승급 화면도 대상 선택 후 "보유 영웅" 재고르기 목록을 중복으로 그리고 있던 걸 대상 미선택 시에만 보이게 정리. 420×740 웹 캔버스가 아니라 실제 설치 앱(상태바·제스처바로 실질 높이가 더 좁음) 기준으로 검토해달란 요청에 맞춰 360×740(여유 없는 실기기 프로필)로 Playwright 측정. 도감(전체 로스터 브라우징 목록)은 스크롤이 자연스러운 화면이라 대상에서 제외. **③정렬**: 보유 영웅 그리드를 성급desc→등급desc(UR>SSR>SR>R>N)→진영(REAL_FACTIONS 순서, 히든 "불명"은 맨 뒤) 3단 정렬로 변경. **④이미지 반영**: ASSETS.md 참고, 2026-07-29에 이미 도착해 있었지만 연결이 안 됐던 9종(class-*, icon-lock/power, mail-*, shop-freebox/gems, skill-attack/defense)을 이모지 자리에 실제 반영, badge-new.png는 "NEW" 텍스트가 없는 빈 리본이라 정보 유실 문제로 보류. Playwright로 hold 반복(1.5초 홀드→8회 레벨업, 릴리즈 시 정지) · 5개 화면 오버플로 재측정(전부 해소, 도감 제외) · 정렬 순서(7명 테스트 데이터로 5★→3★→2★→1★ 순서 확인) · 신규 아이콘 9종 렌더링까지 전체 검증 |

## 차용 후보 상세 (9~12번)

### 9. 승급 전/후 수치 미리보기
가장 저비용·고효과. **그림 에셋 불필요, 텍스트 한 줄**만 추가하면 됨(`renderAscend`에 계산된 %/최대레벨 증가치 표시). 각성 버튼을 누르기 전에 "얼마나 세지는지"를 알려주는 것만으로 재화 소비 결정에 대한 심리적 저항이 줄어듦 — 게임코드 세션이 바로 처리 가능한 항목으로 제안.

### 10. 상성 관계 안내 UI
`DESIGN.md`에 원소 상성(불→바람→물→불, 빛↔어둠)이 이미 수치로 존재하므로 **로직 추가 없이 표시만** 하면 됨. 영웅 상세 모달에 작은 삼각형 다이어그램이나 "이 진영에 강함 / 이 진영에 약함" 텍스트 한 줄이면 충분. SD 스타일에 맞게 코드로 그려도 되고, 아이콘 3~5개(진영 심볼) 조합으로 처리 가능해 신규 이미지 리소스도 최소.

### 11. 필살기(궁극기) 컷인 — **킬러 콘텐츠 1순위 후보**
두 레퍼런스 모두 "일반 전투와 확실히 다른 최상위 연출 등급"을 갖고 있는데, Circle Heroes는 히트당 임팩트(E1~E8)까지만 있고 이 등급이 비어 있다. 히든(Unknown) 등급이나 UR 전용 필살기에 이 연출을 걸면:

- **차별화 포인트**: 등급 체계(N/R/SR/SSR/UR/Unknown)가 이미 세분화돼 있으니, 필살기 컷인을 **UR 이상만** 갖는 특권으로 설계하면 등급 격차의 "손맛"이 확 살아남 — 뽑기 동기 강화.
- **필요 리소스**: 신규 스프라이트 없이도 시작 가능 — ①화면 전체 컬러 틴트(코드), ②기존 이펙트를 스케일업해 재사용, ③카메라 쉐이크 강화(이미 있음), ④캐릭터 초상(이미 있는 `cards-webp` 활용 가능)만으로 1차 버전 가능. 이후 여유 생기면 등급별 전용 슬래시/오라 이펙트(256×256, 기존 E1~E8과 같은 규격)를 추가하면 됨.
- **담당**: 1차는 게임코드 세션 단독으로 가능(연출 로직+기존 에셋 재활용), 2차 고도화 시 디자인리소스 세션에 전용 이펙트 요청.

### 12. 캐릭터 감상 모드
`assets/cards-webp/`에 이미 존재하는 전신 일러스트를 연결만 하면 되는 항목. 다만 SD/치비 무드에 맞춰 "포토모드"보다는 **"인형놀이/디오라마" 감** — 작은 배경(홈/야외 중 하나 재활용) 위에 치비 캐릭터가 통통 튀거나 리액션하는 정도로 가볍게 가는 게 스타일에 맞음. 실사 3D 회전 열람(HoC식)은 우리 아트 방향과 안 맞으니 그대로 차용하지 말 것.

## 킬러 콘텐츠 브레인스토밍

두 레퍼런스가 공통으로 "약하거나 아예 안 하는" 지점 = Circle Heroes가 파고들 틈.

1. **필살기 컷인의 등급 특권화** (위 11번) — 뽑기 등급 격차를 연출로 체감시키는 장치. 두 레퍼런스 다 이걸 등급과 명시적으로 묶지 않음.
2. **오프라인 240시간 방치 보상의 시각적 "선물 개봉감"** — 두 레퍼런스 모두 복귀 시 팝업 하나로 퉁친다(AFK Arena Companions의 "자동사냥 보상" 참고). Circle Heroes는 싱글플레이 로컬전용이라 서버 동기화 부담이 없으므로, 복귀 연출을 좀 더 공들여도(예: 쌓인 골드가 상자 여러 개로 나뉘어 하나씩 터지는 연출) 비용 대비 체감 임팩트가 큼.
3. **히든(Unknown) 등급의 "세계관 밖 존재" 컨셉을 UI 문법 자체에 반영** — 예: 히든 영웅만 카드 테두리가 다른 영웅과 아예 다른 재질(금속/유리 등 별도 셰이더 느낌)로 보이게 하거나, 도감에서 히든만 다른 배경음/사운드 큐를 쓰는 식. 두 레퍼런스 다 최고 등급을 "같은 문법의 최상위 색"으로만 처리해서 정성적 차별화가 약함.
4. **감상 모드(§12)의 무비용 실행** — 이미 있는 에셋을 연결하는 것만으로 "우리만 있는 기능"이 하나 생김. PM 세션 판단만 있으면 되는 항목.
5. **진영 시너지(모노/레인보우)의 시각적 즉시성 강화** — 이미 수치는 있으니(§7), 편성 화면에서 시너지 조건을 채우는 순간 카드 전체가 진영색으로 확 빛나는 등 "맞췄다"는 걸 손맛으로 알려주는 연출을 추가하면 이미 있는 시스템의 체감 재미가 배가됨 — 신규 시스템 없이 연출만으로 차별화.

우선순위는 PM 세션에서 스프린트 계획과 맞춰 정하는 걸 권장 (여기서는 순서를 확정하지 않음).

## 디자인리소스 세션 전달 사항

- **당장 신규 이미지 없이 처리 가능**: §9(승급 프리뷰), §10(상성 안내, 기존 아이콘 재조합), §12(카드 연결) — 게임코드 세션이 기존 자산만으로 먼저 착수 가능.
- **여유 있을 때 요청할 것**: 필살기 등급 전용 이펙트(등급별 슬래시/오라, 기존 `assets/effects/` 256×256 규격 준용 권장, 다만 풀스크린 버전은 더 큰 캔버스 필요 — 정확한 규격은 코드 세션이 시제품 만들어본 뒤 역산해서 `ASSETS.md`에 슬롯 추가 예정).
- **보류(스코프 밖)**: 3D 허브타운 배경, 코스메틱 스킨 세트 — 지금 단계에서 제작 요청 넣지 말 것.

## 다음 액션 — 2026-07-28 PM 결정 완료

- [x] §9(승급 프리뷰), §10(상성 안내) — **즉시 착수, 구현 완료**(`src/ui/screens.ts` `renderAscendPreview`/`openHeroDetail` 매치업 박스)
- [x] 필살기 컷인 — **UR 전용으로 확정, 구현 완료**(`BattleScene.ts` `ultimateCutin`). 등급 분포 불균형(UR 6종 중 절반이 재료용 캐릭터였던 이슈)은 로스터 재조정과 별개 사안이라 컷인 자체는 그대로 진행
- [ ] 감상 모드(§12) — **나중 여유 스프린트로 보류**. 미착수
- [x] §14 허브타운 — 애초 제안(초경량 장식 배경)보다 확장 결정: **2D 클릭형 실동선 허브타운으로 진행**. 3D·소셜기능만 스코프 밖 유지. 1차(코드) 버전 구현 완료(`renderHub`), 실제 배경/건물 일러스트는 디자인리소스 세션 후속 작업

## 진행 기록

| 날짜 | 내용 |
|---|---|
| 2026-07-30 | **§46 레벨업 홀드 반복 + 스크롤 없는 화면 + 정렬 + 이미지 반영(5건 일괄)** — "우선 모든 영역 하나씩" 다음 라운드로 사용자가 실제 사용 중 느낀 5가지를 한 번에 요청: (1) 영웅강화(레벨업)를 누르고 있으면 연속 진행되게, (2) 영웅상세화면이 스크롤 없이 한 화면에, (3) 영웅은 성급>등급>진영 순 정렬, (4) 나머지도 대부분 스크롤 없이(웹이 아니라 실제 폰 설치 기준으로 검토), (5) 제작 중인 이미지가 도착했으면 반영. (1)은 pointerdown 즉시 1회+420ms 후 130ms 간격 자동반복으로 구현했는데, 레벨업마다 모달 전체를 다시 그리는 기존 구조 때문에 방어적으로 걸어둔 `stopLevelHold()`가 반복 중인 자기 자신의 인터벌을 즉시 꺼버리는 버그가 있었음 — 실기기가 없어 Playwright로 실제 1.5초 길게 눌러보는 테스트를 짜서 "Lv.1→Lv.3에서 멈춤"을 직접 재현·발견 후 그 라인만 제거해 해결. (2)+(4)는 처음엔 "설치 앱은 상태바/제스처바 때문에 브라우저보다 실제 여백이 좁다"는 정확한 지적이라 420×740 기존 검증 대신 360×740(여유 없는 실기기 프로필)으로 Playwright 오버플로 측정 스크립트를 새로 짜서 전 화면 스윕 — 영웅상세 모달(246px 초과), 승급 재료선택 중복 목록, 소환 픽업탭, 상점, 임무 화면에서 초과 발견. 상점을 조사하다가 "골드 상점/보석 상점/일일 무료" 서브탭이 heroes/missions와 달리 실제로 화면을 안 바꾸는 죽은 UI였다는 걸 발견(§43에서 잡았던 "확률·천장"과 같은 부류의 버그) — 이번엔 진짜로 8개 항목을 3개 탭에 나눠 담도록 고쳐서 스크롤 문제와 죽은 UI를 동시에 해결. 도감(전체 로스터 브라우징)은 스크롤이 자연스러운 화면이라 대상에서 제외하고 나머지는 전부 스크롤 없이 들어가게 압축 완료. (3)은 `getStars`desc→`GRADE_ORDER`desc→`REAL_FACTIONS` 순 3단 정렬. (5)는 ASSETS.md 조사 중 2026-07-29에 이미 도착했지만 코드에 연결이 안 돼 계속 이모지로 보이던 파일 9종을 발견해 반영, `badge-new.png`는 "NEW" 글자가 없는 빈 리본이라 그대로 못 쓰는 걸 확인하고 재제작 필요로 백로그에 남김. Playwright로 5개 항목 전부 실동작 검증(hold 반복+정지, 오버플로 재측정, 정렬 순서, 신규 아이콘 렌더링) |
| 2026-07-30 | **§45 카드 구조 party-slot식으로 전 화면 통일 + 장비 탭 인벤토리 재구성** — §42~43에서 분리해뒀던 장비 탭 재구성(item ⑤)을 이번 라운드에서 처리. 사용자가 "4열로 줄여달란 건 개수가 아니라 어느 화면이나 카드 모양을 통일해서 일관성+소스복잡도를 줄이고 싶었던 것, 편성화면(party-slot) 구조가 가장 마음에 든다"고 명확히 하고 레퍼런스 스크린샷 4장을 함께 제시하며 "정사각형(레퍼런스처럼)이 좋을지, 이름을 넣은 지금의 직사각형이 좋을지" 의견을 요청. 답으로 party-slot과 동일한 "아이콘+이름+별" 직사각형 구조를 추천·채택(아래 상세 이유는 §45 매트릭스 행 참고) — `.hero-card`를 영웅(보유/도감/재료픽커)과 신규 `buildEquipCard`(장비 인벤토리)가 공유하도록 재작성. 장비 탭은 영웅부터 고르던 기존 화면을 버리고 보유 장비를 등급순으로 훑는 인벤토리 화면으로 전환, 장착/해제는 영웅 상세의 신규 "✨ 장비" 섹션으로 이동. 구현 중 재료픽커 mat-count 배지 겹침, 영웅상세에서 연 장비 모달이 장착 직후 사라지는 self-close 버그 2건을 출시 전에 발견·수정. `.equip-face`(장비 카드 이모지 중앙정렬) CSS 누락도 검증 단계에서 발견해 추가. Playwright로 tsc/build 클린 확인 후 영웅그리드·도감·재료픽커 카드 통일, 장비탭 인벤토리+슬롯필터, 미장착 장비 클릭→장착할 영웅 고르기→장착 완료, 영웅상세 장비섹션→슬롯 클릭→해제(모달 유지 확인)까지 전체 플로우 실동작 검증 |
| 2026-07-30 | **§42~43 UI 8건 피드백 일괄 반영(장비 재설계 제외) — 카드 4열/별 한 줄/UR 반짝임/등급색 비침, 소환·전투·모험 탭 재편** — "테스트해볼게" 이후 받은 8개 항목 피드백 중 7개 구현, 1개(장비 탭 재구성)는 범위가 커서 다음 라운드로 분리. ① 영웅 카드 그리드 5열→4열. ② 카드 하단 좌하단 압축 배지("★N") 대신 별 5개를 한 줄로 축약 없이 전부 표시, 클래스 아이콘 표현방식은 사용자가 "고민하자"로 열어둬 구현 보류(현재 코너 배지 유지) — 다만 별 줄과 자리가 겹쳐서 클래스 배지를 우상단 Lv 배지 아래로 옮기는 레이아웃 수정은 같이 반영. ③ UR 카드 상시 반짝임(box-shadow 펄스, 다른 등급엔 없음 — "ur카드는 항상 번쩍이게" 요청 그대로). ④ 얼굴 이미지 여백에 등급색이 비치도록 `.face` 배경을 투명으로 전환 — 구현 중 `.hero-card .face`가 카드를 거의 다 덮어서 정작 비칠 여백이 없었다는 게 재확인돼(§41에서 이미 한 번 겪은 문제) inset을 넓힌 상태를 그대로 활용. ⑥ "확률탭은 없애고 단일화면으로" 요청 확인 결과 실제로는 이미 단일화면이었고 "확률·천장" 서브탭은 클릭하면 "준비 중" 토스트만 뜨는 죽은 UI였음을 발견 — 그 가짜 탭만 삭제. ⑦ "메뉴를 누르면 항상 대표화면으로" — 영웅/임무 탭은 마지막 서브뷰 대신 매번 기본값으로, 전투 탭은 배틀모드를 강제로 스테이지로 리셋. ⑧ 하위메뉴가 끝내 미정으로 남아있던 주성 탭(6개 건물 자리표시자, §마이티 아레나 반영계획 B) 완전 삭제, 전투 탭 서브메뉴였던 모험 허브를 전투 탭 오른쪽 독립 메인 탭으로 승격 — 재진입 시 항상 픽커부터 다시 보여주고(⑦과 같은 원칙), 배너 선택 시 스테이지 탭처럼 투명해져 Phaser 캔버스가 비침. 구현 중 "컨테이너만 투명하게 하면 픽커가 캔버스 위에 그대로 겹쳐 보인다"는 버그를 Playwright 스크린샷으로 직접 발견해 `innerHTML`까지 비우도록 수정. ⑤(장비 탭을 아이템 인벤토리 중심으로 재구성 + 장착을 영웅상세로 이동 + 장비를 모험/자동사냥 보상에 등급별로 추가)는 기존 장비 시스템(§장비 시스템 v2, §마이티 아레나 반영계획 3) 전체를 재배선해야 하는 별도 규모의 작업이라 이번 라운드에서 분리, 다음 라운드에서 진행 예정. Playwright로 4열 그리드+별 한 줄+UR 발광+등급색 비침(도감 화면), 탭 순서(영웅·소환·전투·모험·상점·임무), 영웅 탭 재진입 시 서브뷰 리셋, 모험 탭 픽커→아레나 선택→캔버스 노출→탭 재진입 시 픽커 복귀→전투 탭 클릭 시 스테이지 강제 복귀까지 전체 플로우 실동작 확인 |
|---|---|
| 2026-07-30 | **§41 카드 표시방식 최종 정리 + 시크릿 코드 테스트 지원** — 직전 라운드에서 "논의만 하고 보류"였던 카드 배경/테두리/성급·초월·진영 표시 방안에 사용자가 결정을 내려 바로 구현. 논의 흐름: 내가 C안(전체 배경=등급색)+진영 좌상단 배지+Option1(등급=배경/초월=테두리) 변형을 제안했고, 사용자가 "테두리는 초월용 색상 축 대신 별 색상 전환으로 흡수하자"(보라 pip 추가 아니라 "황금별6이 보라별1로 표현") 역제안 → 이후 "테두리는 어디 쓰냐"는 질문에 내가 "성급에 재연계하면 중복이니 공통 프레임으로 비워두자"고 답해 최종 확정: **배경=등급색(C안, `setCardGrade`/`GRADE_CARD_BG`), 테두리=등급 구분 없는 공통 프레임(장비 아이템은 예외, 기존 `setGradeBorder` 유지), 진영=좌상단 코너 배지(`addFactionBadge`), 성급+초월=별 하나 공유(초월 시작 시 골드★가 보라✪로 통째 전환+반짝임, `.star-purple`)**. 적용 범위: 보유/편성/도감 그리드 카드, 편성 슬롯, 승급 대상 슬롯, 장비 화면 대상 패널, 영웅 상세 패널, 소환 결과 카드·신규획득 팝업까지 "카드형" 표시 전체(장비 아이템 자체는 스코프 밖, 기존 방식 유지). 구현 중 발견한 문제: `.hero-card .face`가 `inset:3px`라 얼굴 이미지가 카드를 거의 다 덮어 등급색 배경이 보일 여백이 없었음(B안은 box-shadow가 카드 "바깥"이라 무관했지만 C안은 카드 "안" 여백이 곧 배경) — inset을 6~9px로 넓혀 해결. Playwright로 도감 화면(전 등급 한눈에 비교, UR=붉은 톤/SSR=금색 톤 확인) + 영웅 상세(진영 배지·등급 배경 선명하게 확인) 검증. **테스트 지원**: "코드에 1 넣으면 8성 영웅 하나 줘" 요청으로 시크릿 코드 `1` 신설(`grantMaxTestHero`) — 마초(UR)를 5성 만렙+초월3단계로 즉시 지급, Playwright로 `dh-stars` 클래스/텍스트가 `star-purple`/`✪✪✪✩✩`로 정확히 뜨는 것까지 확인 |
| 2026-07-30 | **§37~40 모험 허브 재편 + 소환 일일 무료 1회 + 오프닝 화면 + 소환 확률 재조정** — 전체 메뉴 구조 마인드맵(오프닝/영웅/장비/모험/메인/소환/임무/상점)을 먼저 제시하고 항목별 의견을 물어, 승인분만 구현. **영웅의전당**(레퍼런스 확인 중)과 **가방**은 사용자가 명시적으로 보류 지시해 미구현. **모험 허브**(§37): 전투 탭의 4개 칩(스테이지/요일던전/아레나/무한의탑)을 "스테이지"+"모험" 2개로 축소하고, 요일던전/아레나/무한의탑은 오두막 타일 배너 3개짜리 모달로 분리 — 요일던전 5선택 화면(§24)에서 이미 재활용됐던 문법을 세 번째로 재사용. **일일 무료소환**(§38): "소환권을 주지 말고 버튼 자체가 무료소환 버튼으로 바뀌어야 한다"는 사용자 설계를 그대로 구현 — 정오/자정 경계 반구간 키로 일반·고급 각각 독립 판정, 미사용시 비누적 소멸. 소환 버튼 옆 빨간 점 + 하단 탭바 소환 아이콘 배지(기존 우편함 배지 패턴 일반화, 60초 폴링+이벤트 병행 갱신). **오프닝 화면**(§39): navy/gold 그라디언트 스플래시, 탭하면 진입. **소환 확률 재조정**(§40): 일반소환에 SSR 0.1% 추가, 고급소환은 SR~UR만(N/R 제외), 픽업소환은 SSR 적중 시 100% 확정 지급(50/50 폐지) — 고급소환 쪽엔 사용자 지시대로 보상 차별점을 의도적으로 추가하지 않음. Playwright로 무료소환 전체 사이클 실동작 확인: 소환 진입 시 탭배지+버튼 "무료 소환"+빨간 점 노출 → 소환 실행(연출 스킵→카드 개봉→확인) → 소환 상태 저장(`freeSummonWindow.normal`에 현재 창 키 기록) → 버튼이 "1회 소환(🎫1)"로 즉시 원복 → 고급소환은 아직 미사용이라 탭배지는 그대로 유지되는 것까지 확인. 카드 표시방식(C안 vs 외곽 매트형 B안 재검토)·태생등급-성급-초월-진영 4요소 시각 통합 방안은 사용자가 "논의하자"로 명시해 이번 라운드는 의견 제시만 하고 구현 보류 |
| 2026-07-30 | **§31~36 신규 스크린샷 16장 분석 + 승인분 전면 구현(등급 배경/장비 강화/소환 3분할/승급 재설계/초월/레벨 초기화)** — 마이티 아레나 추가 스크린샷 16장(수련=IAP 퍼널 허브, 환생, 특성탭, 장비 성급 슬롯, 관문 버프, 모험 지도, 확률업 소환 등)을 상세 분석해 신규 요소 카탈로그로 먼저 전달, 승인받은 것만 구현. **등급 배경색**: 카드 내부(진영색 카드+중립 얼굴틀, task #37) 구조를 안 건드리고 카드 바깥에 등급색 매트를 두르는 "외곽 매트형" 채택 — 3가지 방식을 [비교 아티팩트](https://claude.ai/code/artifact/53d10502-ae8e-4526-8773-1bb33a5408cb)로 제시 후 결정. **장비 강화**: 강화석 부활(`save.enhanceStone`), `EquipItem.invested/level` 신설, 강화석 직접 투입 + 다른 장비 흡수(이월) 지원, 전투 보상·상점에 강화석 획득처 추가. **소환 3분할**: 일반/고급/픽업 소환권 2종 경제로 전환(`gacha.ts` 전면 재작성), 픽업소환은 후보 3명 중 선택형(영웅 id 기준 천장). **승급 재설계**: "동일 캐릭터 복제" 요구를 폐지하고 "동일 성급 아무 영웅 2체"(4→5성만 진영 일치)로 — 재료 선택 UI 신설, 후보 클릭=추가/재료슬롯 클릭=제거 인터랙션으로 설계(최초엔 토글식으로 짰다가 "같은 영웅 2장 선택이 안 되는" 버그 발견해 수정). **초월**: 5성 이후 보라색 별 트랙(1단계=동일 영웅 5성 완본 소모, 2~5단계=1성 영웅 2장씩) 신규 구현. **레벨 초기화**: 환생 대신 레벨업 골드만 전액 환급, 위치는 임시로 영웅 상세화면. Playwright로 승급(재료 2장 선택→각성 성공), 장비 강화(강화석 투입+장비 흡수 이월 계산 검증), 픽업소환 화면, 레벨 초기화 확인 다이얼로그, 상점 소환권/강화석 판매까지 전부 실동작 확인 |
| 2026-07-29 | **§25~30 마이티 아레나 반영계획 A/B/D/E/F/I 구현** — "가장 마음에 든 레퍼런스"라며 GitHub Release(`mighty_ref`)로 61초 실 게임플레이 영상을 올려주셔서 ffmpeg 프레임 추출로 상세 분석 후 반영계획을 먼저 제시, 승인받은 항목만 구현. **A** HUD 골드→다이아→전투력 순서+골드/다이아 클릭 시 상점 이동. **B** "주성" 탭 신설 — 3D는 채택 안 하고 기존 2D 오두막 타일 문법 재사용, 하위메뉴 내용 미정이라 건물 자리 6개(`FEATURE_GATES`)만 인프라로 배치, 클릭은 "준비 중" 토스트. **D** 스테이지 게이트+상단 슬라이드 배너(`.gate-banner`) 신설 — 이미 열려 있던 기존 기능은 소급 잠그지 않고 §27 주성 건물에만 적용. **E** 소환화면 좌상단 확률고지/우상단 도움말+원형 배지 — "소환 포인트 대신 기존 천장시스템을 적용해달라"는 지시대로 신규 재화 없이 `UR_PITY_LIMIT` 진행률을 conic-gradient 링(`.pity-ring`)으로 시각화. **F** 아레나 랭킹(서버 매칭 전제)은 제외하고 마일스톤 트랙 구조만 떼어 주간 임무에 재적용(`WEEKLY_MILESTONE_TRACK`). **I** 온라인 tick 적립 방식은 유지하고 오프라인 보상 모달에 "최대 240시간" 표기만 추가. 실화폐 결제(영웅 판매/개발자 후원)는 플레이스토어 출시 시점으로 보류. 주성 탭 아이콘(`tab-castle.png`)은 PIL로 급조한 자리표시자 — 정식 일러스트는 ASSETS.md 백로그. Playwright로 HUD 재정렬, 골드 클릭→상점 이동, 주성 잠금/해금 표시, 소환 확률고지·도움말·천장 링, 주간 마일스톤 5구간 전부 실동작 확인 |
| 2026-07-29 | **§14/§24 요일던전 카운터 진영 규칙 + 5던전 선택 화면** — "바람의 마수면 바람을 이기는 불만 출전해야지"로 출전 규칙을 같은 진영→상성 진영으로 변경, "5가지 요일던전을 화면에, 요일 맞춰 활성화·나머지 잠금"으로 선택 화면 신설. "벤치마킹 가능한 곳 있으면 해보라"고 하셨으나 레퍼런스 20장엔 해당 화면 유형이 없어서, 대신 "마을뷰를 여기에 쓰면 되겠다"는 제안대로 §80에서 실사용 제거했던 허브타운의 오두막 타일 코드를 git 히스토리(`172e592`)에서 되살려 재활용. 결과적으로 §14는 "폐기"가 아니라 "다른 화면으로 이식됨"으로 매트릭스 갱신 |
| 2026-07-29 | **§21 마일스톤 포인트 트랙 구현 + 사운드 인프라 신설** — "다음은 뭐할까" 질문에 사운드(오디오 전무 확인)와 §21 마일스톤 트랙 2건을 제안해 "진행하자"로 확정, 병행 착수. §21은 일일 임무 5개×20점=100점 설계로 완성(`missions.ts` `dailyPoints`/`milestoneClaim`). 사운드는 아직 음원 파일이 없어 매트릭스에는 반영 안 함(레퍼런스 스크린샷만으론 오디오를 비교할 수 없음) — 대신 재생 인프라(`src/systems/audio.ts`)와 ASSETS.md 규격표(BGM 1+SFX 13종)만 먼저 준비해 디자인 세션에 병행 요청 |
| 2026-07-29 | **§22 장비 5슬롯 → 6슬롯 확장(목걸이/반지 분리) + HUD 버그 수정** — "5종인데 6종이면 뭐가 다르냐"는 질문에 레퍼런스에서 아이콘으로 확인 가능했던 4종(무기/신발/갑옷/반지)과 장르 관례(장신구를 목걸이+반지로 분리)를 설명, "늘리자"로 확정 반영. 목걸이=치명타 확률(기존 장신구 자리), 반지=치명타 피해(신규, `EQUIP_GRADE_PCT` 재사용해 무기/투구/갑옷과 같은 배율 방식). 6슬롯을 420px 한 줄에 넣기 빡빡해서 `.equip-slot-grid`를 3열×2행 CSS grid로 변경. 겸사겸사 장비 장착 시 HUD 전투력 칩이 갱신 안 되던 버그(`refreshHud`가 `"equipment-changed"` 이벤트를 구독 안 하고 있었음, MVP 때부터 있던 버그)를 6슬롯 전량 테스트하다 발견해 같이 수정 — Playwright로 전투력 실시간 반영(880→1,018) 확인 |
| 2026-07-29 | **§22 장비 시스템 v2 재설계(강화 방식 → 획득+장착 방식)** — 방금 만든 3슬롯 강화 레벨 시스템을 주인님이 다시 보고 "무기/투구/갑옷/신발/장신구 5종, 등급별 장비 획득+장착 뷰"로 재설계 요청, 레퍼런스 벤치마킹 지시. `reference/`의 `03-hero-detail-equipment.jpg`(AFK Arena Companions)와 `02-hero-detail-equipment.jpg`(HoC Legends)를 직접 열어 확인한 결과 둘 다 "인스턴스 아이템을 인벤토리에 모아 슬롯에 장착"하는 동일 문법이라 그대로 차용, 좁은 세로 캔버스에 맞춰 캐릭터 좌우 6슬롯 대신 가로 5열 행으로 배치만 조정. 강화석 재화·강화 함수 전부 제거하고 인스턴스 아이템(`EquipItem`)+인벤토리(`equipInventory`)+장착맵(`equipped`)으로 교체 |
| 2026-07-29 | **구조적 개선 4건(§22 장비 시스템, §23 타워 다양화, 고스트 UI 정리, 강화석 재화)** — "디자인만 입히면 확장판 느낌이 날까?" 질문에 룩앤필은 수렴했지만 콘텐츠 깊이는 별개라고 답하며 발견한 4가지 갭을 전부 반영. `DESIGN.md`에 원래 설계돼 있었지만 미구현 상태였던 장비 시스템(§22)을 승급과 같은 패턴(대상 선택+슬롯 강화)으로 신설, 무한의 탑(§23)을 스테이지 지역전환(§76) 로스터 재사용으로 다양화, 장비/이벤트/상점의 죽은 UI 3곳 정리, 강화석 재화 신설(전투 보상+상점 교환). Playwright로 강화 실행(골드/강화석 차감 확인), 상점 구매, 20층 타워 몬스터 테마 전환(플레이스홀더 폴백 정상)까지 실동작 검증 |
| 2026-07-28 | **버튼 위치·영웅 비율·UI 로직 정밀 반영** — "AFK 아레나2로 벤치마킹" 요청에 앱스토어/위키/리뷰사이트 직접 크롤링은 전부 403으로 막혔으나, 이 문서 §레퍼런스자료 표에 이미 링크돼 있던 아티팩트(`claude.ai/code/artifact/...`)는 WebFetch로 정상 열람 확인 — 위 표의 "AFK Arena: Companions"(2D 유화 스핀오프, 정식 "AFK Arena 2"/AFK Journey와는 다른 별도 타이틀) 분석이 픽셀 단위 크기·비율·배치 좌표까지 포함한 매우 상세한 자료였음. 재대조 결과: 하단 5탭+중앙확대, 별도 하단CTA, 승급프리뷰, 소환천장카운터, 필살기컷인 등 기존 반영분은 이미 이 자료 기반이라 대부분 일치 확인. 신규 반영: 파티원 카드 Lv뱃지를 하단 텍스트줄 → **좌상단 코너뱃지**로, 보유영웅 카드 "출전 중" 표시를 하단 텍스트줄 → **좌상단 대각선 리본**으로 변경(리포트의 "좌상단 Lv뱃지"/"좌상단 출전 태그" 규칙 적용). 영웅 초상 크롭 비율(정사각 얼굴클로즈업 vs 리포트의 세로 5:7~4:7 포트레이트 프레임)은 이전 실기 피드백 확정 사항(task #24)과 상충해 보류, 주인님 확인 필요 |
| 2026-07-28 | **PM 세션 결정 반영 + 4개 항목 구현 완료** — §9(승급 스탯 프리뷰)·§10(상성 안내 UI)·필살기 컷인(UR 전용)·§14(2D 클릭형 허브타운, 3D/소셜만 스코프 밖 유지로 범위 확장) 전부 즉시 착수 결정 받아 구현. 감상 모드(§12)는 보류. Playwright로 허브 진입→건물 클릭→탭 이동→복귀, 영웅 상세 상성 표시, 승급 프리뷰, 전투 중 UR 필살기 컷인(초상화 확대+진영색 버스트+암전+텍스트)까지 전부 실동작 확인 |
| 2026-07-29 | **`reference/` 원본 스크린샷 폴더 발견·병합** — 다른 세션(`claude/afk-arena-game-analysis-15a7o7` 브랜치)이 전날 이 문서 §레퍼런스자료의 아티팩트 분석을 뒷받침하는 **실제 원본 이미지**(AFK Arena Companions 영상 프레임 추출 14장 + HoC Legends 캡처 6장, 전부 1080×2340 원본 해상도)를 만들어 별도 브랜치에 올려둔 걸 발견해 작업 브랜치에 병합(`circle-heroes/reference/`). PROMPTS.md "영웅 화면" §6(전투 유닛 크기·간격)·§10(초상화 클릭 분기)이 "레퍼런스 없이는 확인 불가"로 보류돼 있던 상태라 이걸로 검증:
  - **§10 해소**: `05-hero-codex-grid.jpg`의 "롱프레스 시 상성 팝업" 케이스처럼, 레퍼런스는 짧은 탭=상세 이동, 롱프레스=퀵 팝업으로 구분하는 문법을 씀. Circle Heroes는 보유·편성/승급/도감 전부 탭 한 번으로 상세 이동(§10 이전 작업에서 상성 정보를 상세화면 안에 이미 포함시켜놔서 별도 롱프레스 팝업 없이도 정보 접근은 동등) — 레퍼런스와 어긋나지 않는 것으로 확인, 블로커 해제.
  - **§6 검증**: `01-pre-battle-formation.jpg`(1080×2340) 기준 전열 아군 x≈31%, 적군 x≈70% 지점에 배치됨. `BattleScene.ts`의 `HERO_SLOTS`(전열 x=132/420=31.4%)·`enemyX`(312/420=74.3%)와 이미 3~4%p 이내로 근접 — 별도 조정 없이 검증 완료로 종결. (레퍼런스는 유닛을 오각형/다이아몬드꼴로 배치하는데 Circle Heroes는 2열 컬럼 배치라 **포메이션 형태 자체**는 다름 — 이건 "크기·간격 비율"이 아니라 구조 변경 사안이라 이번 범위 밖으로 남겨둠, 필요시 별도 논의)
  - **§9(재확인)**: `05-hero-codex-grid.jpg`는 4열 그리드+미보유는 완전 빈 배너(실루엣도 안 보여줌)인데, Circle Heroes 도감(`renderCodex`)은 5열+미보유 흑백 실루엣(수집 동기 부여 목적으로 의도적으로 다르게 감) — 표절 리스크 없는 의도적 차이라 유지 |
| 2026-07-28 | 최초 작성 — AFK Arena: Companions(영상 3부) + HoC Legends(스크린샷 7장) 분석 기반. 코드 교차검증(승급 프리뷰 없음, 상성 UI 없음, HP바/천장카운터/시너지표시는 이미 구현 확인)까지 마침 |
| 2026-07-28 | `reference/` 폴더에 원본 스크린샷 20장(afk-arena-companions 14장 + hoc-legends 6장, 전부 1080×2340) 커밋 — PM 세션이 버튼 좌표·영웅 스프라이트 비율을 직접 픽셀 단위로 잴 수 있게. 좌표 변환식은 `reference/README.md` 참고 |
