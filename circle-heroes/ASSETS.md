# Circle Heroes — 이미지 에셋 규격서 (Rev.B — 애니메이션풍)

주인님이 이미지 파일을 제작·제공하면 Claude가 이 규격에 맞춰 게임에 조립한다.

> **Rev.B 변경사항**: 아트 방향이 도트(픽셀아트)에서 **애니메이션풍 일러스트**로 변경됨에 따라 전면 개정.
> 도트는 작게 그려 확대하는 방식이었지만, 애니풍은 **크게 그려서 게임이 축소**하는 방식이다.
> 원본이 게임 표시 크기의 2배 이상이어야 고해상도 폰에서 또렷하다.

## 공통 규칙

- **캐릭터/아이콘**: PNG + 투명배경. **배경 그림**: PNG 또는 JPG 무관.
- 아래 표의 크기 **이상**으로 제작 (더 커도 됨 — 축소는 제가 처리. 작으면 흐릿해져서 못 씀).
- 캐릭터는 **오른쪽을 바라보는 방향** 기준 (왼쪽 보기는 코드에서 반전).
- 광원 왼쪽 위 통일. **그림체(선 굵기·채색 톤·눈 스타일)를 전 캐릭터 동일하게 유지** — AI 생성 시 스타일 프롬프트를 고정해두는 걸 추천.
- 등급 테두리, 별점, 번쩍임/홀로그램 연출, NEW! 배지 등은 Claude가 코드로 입힘 — **그림만** 주면 됨.
- 전달: 채팅에 파일 첨부 + 어느 슬롯의 누구인지 한 줄. 저장 경로·파일명은 Claude가 처리.

## 슬롯별 규격 (권장 제작 크기)

| # | 슬롯 | 제작 크기(px) | 게임 표시(대략) | 쓰이는 곳 · 비고 |
|---|---|---|---|---|
| 1 | 영웅 전투 캐릭터 | **512 × 512** | 100~140px | 전투·탑·아레나. 몸 전체, 발이 캔버스 하단 중앙. 정지 1장이면 통통 튀는 idle 연출은 코드로 처리 |
| 2 | 영웅 카드 일러스트 | **1024 × 1536** (세로 2:3) | 카드 전체 | 상세화면 카드·가챠 연출·도감. 목업의 "ELARA" 카드처럼 상반신~전신 |
| 3 | 영웅 아이콘 | **256 × 256** | 56~72px | 편성 그리드·소환 결과 목록. 얼굴 위주 (카드에서 크롭 가능 — 요청 시 Claude가 처리) |
| 4 | 몬스터 전투 캐릭터 | **512 × 512** | 90~120px | 웨이브 일반 몬스터 (고블린, 슬라임 등) |
| 5 | 보스 캐릭터 | **768 × 768** | 160~220px | 스테이지 보스·탑 보스 (목업의 드래곤 포지션) |
| 6 | 장비 아이콘 | **256 × 256** | 48~64px | 장비 인벤토리·강화. 히든 장비 포함 |
| 7 | 재화 아이콘 | **128 × 128** | 24~32px | 골드·보석·소환티켓 등 HUD |
| 8 | 스킬 아이콘 | **256 × 256** | 48~72px | 전투 하단 스킬바(목업 하단 파란 아이콘들)·영웅 상세 |
| 9 | 전투 배경 | **1080 × 1920** (세로 확정) | 전체 화면 | 세로 화면 확정. 위쪽 1/4은 HUD에 가려짐 |
| 10 | 로비/길드홀 배경 | 슬롯 9와 동일 | 전체 화면 | 목업의 "Adventurer's Guild" 같은 홈 화면 |
| 11 | 게임 로고 | **1024 × 512** | 타이틀 | 투명 배경 |
| 12 | 앱 아이콘 | **1024 × 1024** | 런처 | 투명 아님, 꽉 찬 정사각형. 라운딩은 OS 처리 |
| 13 | (선택) UI 패널/버튼 | 자유 | — | 처음엔 Claude가 코드로 그림. 나중에 이미지로 교체 가능 |

## 파일 이름 규칙 (Claude가 저장할 때 사용)

```
assets/sprites/heroes/<영웅id>/battle.png   ← 슬롯1
assets/sprites/heroes/<영웅id>/card.png     ← 슬롯2
assets/sprites/heroes/<영웅id>/icon.png     ← 슬롯3
assets/sprites/enemies/<몬스터id>.png       ← 슬롯4·5
assets/sprites/items/<장비id>.png           ← 슬롯6
assets/sprites/ui/<이름>.png                ← 슬롯7·8·11
assets/backgrounds/<장소/테마>.png           ← 슬롯9·10
```

영웅 id는 Notion 마스터데이터의 `id` 컬럼과 일치 (예: `mage_star_001`).

## 지금 당장 필요한 것 (프로토타입 교체분)

1. 영웅 전투 캐릭터 3종 — 이그니스(불 탱커)·실피(바람 궁수)·클라라(빛 사제) — 512×512 ×3
2. 일반 몬스터 1종 (슬라임 또는 고블린) — 512×512
3. 보스 1종 (슬라임 킹 또는 드래곤) — 768×768

이 5장이면 전투 화면이 목업처럼 보이기 시작한다. 캐릭터 콘셉트는 그림에 맞춰 마스터데이터를 고치면 되므로 자유롭게 제작해도 됨.

> **주의**: 위 "파일 이름 규칙"의 `assets/sprites/...` 중첩 경로는 초기 설계 당시 문서다 — 실제로는
> `vite.config.ts`의 `SERVED_ASSET_DIRS`가 `assets/{characters,monsters,backgrounds,icons,effects,cards-webp}/`
> 를 평평하게 서빙하는 방식으로 굳어졌다(2026-07-25 진행 중 변경, 이 문서 미반영). 아래 스테이지
> 지역 전환 목록은 **실제 현재 규칙**(평평한 폴더 + 파일명만)을 따른다.

## 스테이지 지역 전환 아트 (2026-07-29 신규 백로그, 미제작)

`src/data/stageTiers.ts`에 이미 데이터/코드 배선은 끝났다 — 아래 파일을 지정된 이름 그대로
`assets/backgrounds/`, `assets/monsters/`에 추가하기만 하면 코드 수정 없이 즉시 해당 스테이지
구간부터 자동 적용된다(텍스처가 없는 동안엔 조용히 1티어 초원/슬라임으로 대체되므로 순차 제작 가능 —
전부 한 번에 만들 필요 없음).

| 구간(스테이지) | 지역명 | 배경 파일(1080×1920) | 일반 몬스터(512×512) | 보스(768×768) |
|---|---|---|---|---|
| 1~9 | 초원 | `battle-grassland.png` ✅ 완료 | `slime_green_001.png` ✅ 완료 | `boss_slime_001.png` ✅ 완료 |
| 10~19 | 어둠숲 | `battle-forest.png` | `wolf_001.png` | `boss_direwolf_001.png` |
| 20~29 | 수정동굴 | `battle-cave.png` | `bat_001.png` | `boss_golem_001.png` |
| 30~39 | 화산지대 | `battle-volcano.png` | `imp_001.png` | `boss_salamander_001.png` |
| 40~49 | 빙하설원 | `battle-frozen.png` | `frost_wolf_001.png` | `boss_yeti_001.png` |
| 50+ | 심연 | `battle-abyss.png` | `wraith_001.png` | `boss_demonlord_001.png` |

구간 폭(10스테이지)·지역 콘셉트는 코드 세션 1차 제안이라 마음에 안 들면 언제든 조정 가능
(`stageTiers.ts`의 `minStage`/`name`만 바꾸면 됨, 아트 파일명 규칙은 유지 권장).

## 영웅 공격 포즈 일러스트 (2026-07-29, "공격모션 B안" 채택 — 진행 확정)

`PROMPTS.md`의 공격 모션 4안 비교에서 **B안(포즈 추가 일러스트)으로 진행 확정**. 코드 쪽 하이브리드
배선은 이미 끝났다(`BattleScene.ts` — 공격 타이밍에 `<영웅id>-attack.png`가 있으면 잠깐 그걸로
바꿔치기하고 없으면 지금처럼 정지 그림 스쿼시&스트레치만 그대로 유지). **영웅별로 하나씩 도착하는
대로 순서 상관없이 바로 활성화**되니, 71종을 한 번에 몰아서 만들 필요 없다.

- **파일명**: `assets/characters/<영웅id>-attack.png` (기존 `<영웅id>.png` 정지 초상화와 같은 폴더)
- **크기**: 512×512 (기존 정지 초상화와 동일 규격)
- **포즈**: 무기를 휘두르거나 찌르는 순간처럼 "타격 임팩트 정점" 한 컷. 정지 초상화와 같은 캐릭터
  디자인·색·비율 유지(같은 그림체여야 두 이미지 전환이 어색하지 않음), 오른쪽을 보는 방향 기준
  (왼쪽 보기 예외는 코드가 자동 반전 처리하므로 그림은 항상 오른쪽 기준으로 제작)
- **대상**: 영웅 71종 전원(`data/master/heroes.json`의 `id` 컬럼 기준). 우선순위 필요하면 등급
  높은 순(UR→SSR→SR)이나 인기 영웅부터 먼저 요청하는 것도 가능

## UI 아이콘/버튼 (2026-07-29, 전수조사 + 신규 백로그)

캐릭터/배경 말고 **화면 자체를 이루는 자잘한 아이콘·버튼**을 코드 세션이 전수조사했다. 기존 산출물
5개 중 2개(버튼 배경 2종)는 실제로 코드에 연결까지 마쳤고, 나머지는 지금 이모지(⚔️🛡️✨🔒 등)로
때워둔 상태라 실제 그림으로 바꾸면 바로 체감 품질이 오른다. 전부 `assets/icons/`에 파일명만
맞춰 넣으면 코드 수정 없이 바로 적용되게(또는 최소 수정만 필요하게) 준비해뒀다.

### 이미 반영 완료 (추가 작업 불필요)
- `btn-primary-bg.png`(금색 캡슐 버튼), `btn-danger-bg.png`(적색 캡슐 버튼) — 이번에 실제로
  9-slice로 코드에 연결함. 모든 주요 CTA 버튼과 "세이브 초기화" 버튼에 적용 완료.
- **2026-07-30 반영 9종** — "이미지 제작중, 들어온 이미지 있으면 반영해달라"는 요청으로
  `assets/icons/`를 다시 훑어보니 아래 9개 파일이 2026-07-29에 이미 도착해 있었는데
  코드에 연결이 안 된 채로 남아있던 걸 발견, 전부 이모지 자리에 실제 이미지로 교체:
  `class-dealer.png`/`class-tanker.png`/`class-support.png`(영웅 카드 클래스 배지),
  `icon-lock.png`(미보유 카드 잠금), `icon-power.png`(전투력 칩 2곳), `mail-item.png`/
  `mail-notice.png`/`mail-normal.png`(우편함 종류 아이콘), `shop-freebox.png`(일일 무료 상자),
  `shop-gems.png`(보석 패키지), `skill-attack.png`/`skill-defense.png`(영웅 상세 스킬 아이콘).
  화질·스타일 전부 기존 `elem-*.png`/`tab-*.png`와 잘 어울리는 플랫 SD 톤으로 확인 후 적용.

### 재작업 필요 (파일은 있지만 모양이 용도에 안 맞음)
- `chip-bg.png` — 지금 파일은 동그란 3D 렌즈/조약돌 모양인데, 실제 쓰이는 곳(하단 서브메뉴 칩)은
  버튼처럼 **캔버스 좌우 끝까지 꽉 찬 가로 캡슐** 모양이어야 9-slice가 가능하다. `btn-primary-bg.png`
  와 같은 구도(가로 캡슐, 좌우 끝까지 채움)로 다시 제작해주면 바로 연결 가능. 640×192 사이즈는 유지.
- `icon-close.png`, `icon-speed.png` — 파일 자체는 정상(투명배경 확인함)이지만, 코드에서 텍스트
  버튼("닫기", "▶ x2")을 이미지+숫자 조합으로 바꾸는 별도 작업이 필요해서 이번엔 보류. 원하시면
  다음에 바로 붙일 수 있음.

### 신규 필요 목록 — 지금 전부 이모지로 대체 중인 것들

이번 라운드(2026-07-30)에 실제로 도착해 있던 9종(class-dealer/tanker/support, icon-lock,
icon-power, mail-item/notice/normal, shop-freebox, shop-gems, skill-attack/defense)은 위
"이미 반영 완료"로 옮겼다. 아래는 아직 파일이 없거나, 파일은 있어도 그대로 못 쓰는 것들.

| 우선순위 | 파일명 | 크기 | 대체할 것 | 쓰이는 곳 |
|---|---|---|---|---|
| 중간 | `icon-check.png` | 128×128 | (현재 없음 — 미구현) | 원래 "편성중 카드 체크 배지"용으로 요청했지만, 지금 코드에는 그 자리에 체크 아이콘이 전혀 없다(편성/선택 표시는 카드 테두리 색만으로 함). 이 아이콘을 실제로 쓰려면 카드에 체크 배지를 추가하는 기능 작업이 먼저 필요 — 이미지만 넣는다고 반영되지 않음, 다음 라운드에 UI 작업과 같이 진행 권장 |
| 높음 | `star-filled.png` | 64×64 | ★ | 영웅 상세·승급 화면 성급 표시 |
| 높음 | `star-empty.png` | 64×64 | ☆ | 영웅 상세·승급 화면 성급 표시(빈 칸) — 성급/초월 통합 표시(§카드 표시방식 재정리)라 `star-filled`와 함께 별 5칸 전체를 이미지로 바꾸는 구조 변경이 필요, 텍스트(★☆) → 이미지 전환은 다음 라운드로 미룸 |
| 중간 | `shop-equipbox.png` | 128×128 | 🎁 | 상점 — 장비 상자(§장비 시스템 v2 2026-07-29 신설). "일일 무료 상자"용 `shop-freebox.png`(보물상자 그림)와는 다른 아이템이라 그대로 재사용하지 않음 |
| 중간 | `badge-new.png` | 256×128 | (텍스트 "NEW") | 소환 결과 신규 획득 배지 — **2026-07-30 확인 결과 이미지가 도착했지만 그대로 못 씀**: 받은 파일이 "NEW" 글자 없는 빈 금색 리본 그림이라, 지금 코드가 보여주는 실제 정보("NEW!" 텍스트)가 사라진다. 리본 위에 텍스트를 얹는 CSS 오버레이로 쓰거나, "NEW" 글자가 박힌 버전으로 재제작 필요 |
| 낮음 | `icon-photo.png` | 128×128 | 🖼 | 영웅 카드 일러스트 열람 가능 힌트 배지 |
| 낮음 | `icon-key.png` | 128×128 | 🔑 | 설정 화면 "코드 입력" 섹션 제목 |
| 낮음 | `equip-weapon.png` | 128×128 | ⚔️ | 장비 화면(§장비 시스템 v2 2026-07-29 신설, 6슬롯) — 무기 슬롯 아이콘(공격력) |
| 낮음 | `equip-helmet.png` | 128×128 | 🪖 | 장비 화면 — 투구 슬롯 아이콘(체력) |
| 낮음 | `equip-armor.png` | 128×128 | 🛡️ | 장비 화면 — 갑옷 슬롯 아이콘(방어력) |
| 낮음 | `equip-shoes.png` | 128×128 | 👟 | 장비 화면 — 신발 슬롯 아이콘(속도) |
| 낮음 | `equip-necklace.png` | 128×128 | 📿 | 장비 화면 — 목걸이 슬롯 아이콘(치명타 확률) |
| 낮음 | `equip-ring.png` | 128×128 | 💍 | 장비 화면 — 반지 슬롯 아이콘(치명타 피해) |
| 낮음 | `equip-item-N/R/SR/SSR/UR.png` | 128×128 | (등급 텍스트만) | 인벤토리·장착 팝업의 장비 아이템 아이콘 5종(등급별 톤 차등, 슬롯 공통 사용 가능) — §2026-07-30 장비탭 재구성 후엔 카드에 슬롯 이모지(`equip-weapon.png` 등 위 6종과 동일 모티프)를 등급색 배경(`.hero-card` 공용 배경) 위에 얹는 방식으로 확정, 위 슬롯 아이콘 6종이 준비되면 그대로 대체되고 이 5종은 불필요해짐(취소 검토) |
| 중간 | `tab-castle.png` | 512×512 (기존 `tab-*.png`와 동일 규격) | (PIL로 급조한 자리표시자 — 골드 링+네이비 배경+탑 실루엣) | 주성 탭 아이콘(§마이티 아레나 반영계획 B, 2026-07-29 신설). 지금 파일은 코드로 그린 임시 도형이라 `tab-hero.png`/`tab-battle.png`처럼 AI 일러스트 톤의 정식 배지로 교체 필요 |
| 중간 | `ticket-normal.png` | 128×128 | 🎫 | 일반소환권 아이콘(§마이티 아레나 반영계획 4, 2026-07-30 신설) — 소환화면·상점 |
| 중간 | `ticket-premium.png` | 128×128 | 🎟️ | 고급소환권 아이콘 — 소환화면·상점 |
| 중간 | `icon-stone.png` | 128×128 | 💠 | 강화석 아이콘(§장비 강화, 2026-07-30 재도입) — 상점·강화 모달 |
| 낮음 | `star-transcend.png` | 64×64 | ✪(보라색) | 초월(§마이티 아레나 반영계획 5) 전용 별 아이콘 — 기존 금색 `star-filled.png`와 시각적으로 구분되는 보라색 계열로. 승급 화면 초월 단계 표시에 사용 |

모두 기존 아이콘들(`elem-*.png`, `tab-*.png`)과 같은 폴더(`assets/icons/`)에 같은 파일명 규칙으로
넣으면 된다. 그림 스타일은 `tab-battle.png`처럼 **선명한 색+굵은 외곽선의 플랫한 SD 아이콘**
톤으로(글로시 3D 웹버튼 느낌 아님) 맞춰주면 기존 탭바/진영 아이콘과 잘 어울린다. 우선순위 "높음"만
먼저 만들어도 체감이 크게 달라지고, 나머지는 순서 상관없이 하나씩 넣어도 된다(전부 이모지 폴백이라
파일이 없는 동안은 지금처럼 그대로 보임 — 급하게 다 만들 필요 없음).

## 모험 배너 아트 (2026-07-30 신규 백로그, 미제작)

"모험 배너는 지금 샘플이지만, 가로로 한줄에 하나씩 길게 배너를 넣자 ... 마이티아레나 레퍼런스를
참고해" 요청으로 모험 탭을 2열 오두막 그리드에서 **세로로 쌓인 가로 긴 배너 3장**(요일던전/
아레나/무한의탑) 레이아웃으로 코드를 먼저 바꿨다. 지금은 진영별 그라디언트+사선 패턴을 CSS로
그린 자리표시자라 이모지 아이콘(⚔️🏆🗼)이 그대로 보인다 — 실제 일러스트로 교체하면 체감이
크게 오르는 지점.

| 우선순위 | 파일명 | 크기 | 대체할 것 | 쓰이는 곳 |
|---|---|---|---|---|
| 높음 | `banner-raid.png` | 664×184(약 3.6:1 가로 긴 배너, 모험 탭 카드 실측 332×92의 2배) | CSS 그라디언트(빨강/주황)+⚔️ | 모험 탭 — 요일던전 배너. 마수(진영별 몬스터)가 전면에 크게 나오는 구도 제안 |
| 높음 | `banner-arena.png` | 664×184 | CSS 그라디언트(보라)+🏆 | 모험 탭 — 아레나 배너. 대전/트로피 모티프 |
| 높음 | `banner-tower.png` | 664×184 | CSS 그라디언트(청록)+🗼 | 모험 탭 — 무한의 탑 배너. 탑을 오르는 구도 제안 |

마이티 아레나처럼 배너 안에 캐릭터 일러스트가 꽉 차게 들어가는 톤을 참고하되, 좌측(또는 배경)에
아이콘/캐릭터, 우측에 텍스트(제목+설명)가 겹치지 않게 여백을 남겨줘야 한다(지금 코드가 그 자리에
직접 타이틀·설명·화살표 텍스트를 얹는 구조). 3장 다 세로 92px 높이 규격 통일 필수 — 하나라도
비율이 다르면 리스트가 들쭉날쭉해진다.

## 버튼 UI 전수 점검 (2026-07-30)

"소환버튼이나 임무달성 수령버튼등 버튼이없는곳이 많아. 전체 정비해서 버튼제작 의뢰하자
리스트업해" 신고로 코드를 훑었다.

**코드로 고친 것** — 마일스톤(일일/주간) 보상상자가 `onclick`만 붙은 `<div>`라 클릭 가능
여부가 시각적으로만(테두리 색) 드러나고 버튼으로서의 접근성(포커스/disabled 상태)이 없었다.
실제 `<button>` 요소로 교체하고 `disabled` 상태를 부여해 수정 완료(추가 에셋 불필요).

**나머지 CTA는 이미 커스텀 버튼 아트 적용됨** — 소환 1회/10회, 무료소환, 임무·업적·상점
"수령/구매" 등 주요 CTA는 전부 `.btn`/`.btn primary` 공용 클래스를 쓰고, 위 "UI 아이콘/버튼"
섹션에서 이미 `btn-primary-bg.png`/`btn-danger-bg.png` 9-slice 배경이 적용 완료 상태다(모든
주요 CTA·위험 버튼 포함). 즉 "버튼처럼 안 보이는" 문제의 실체는 이 마일스톤 상자 건 하나였고,
나머지 아이콘류(닫기 X, 소환권 아이콘 등) 백로그는 위 "신규 필요 목록" 표에 이미 있다 — 여기서
중복 등록하지 않음.

## 사운드(BGM/SFX) 규격서 (2026-07-29 신규 백로그)

지금 Circle Heroes엔 **오디오가 전혀 없다**(코드에도 파일에도 0) — 타격감 연출은 꽤 공들였는데
무음이라 체감 완성도가 크게 깎이는 지점이었음. 코드 쪽 재생 인프라(`src/systems/audio.ts`,
`playSfx`/`playBgm`)는 이번에 미리 배선해뒀다 — 기존 이미지 텍스처와 똑같이 **"파일이 있으면
재생, 없으면 조용히 무시"** 방식이라, 아래 파일명 그대로 `assets/audio/`에 넣기만 하면 코드
수정 없이 바로 소리가 난다.

### 파일 형식/이름 규칙
- **형식**: mp3 (다른 포맷 필요하면 코드 쪽에 알려주면 확장 가능)
- **경로**: `assets/audio/<파일명>.mp3` (다른 에셋 폴더와 동일하게 평평한 구조)
- BGM은 `bgm-`, 효과음은 `sfx-` 접두사

### BGM (1곡, 우선순위 높음)

| 파일명 | 길이 | 설명 |
|---|---|---|
| `bgm-battle.mp3` | 1~2분 루프 | 전투 화면이 항상 백그라운드에 떠 있어서(탭 전환과 무관) 사실상 **앱 전체의 메인 테마**가 된다. 밝고 경쾌한 판타지 오케스트라/칩튠 계열, 루프 지점이 어색하지 않게(끊김 없는 시작~끝) |

### SFX (13종)

| 파일명 | 우선순위 | 트리거 위치 | 설명 |
|---|---|---|---|
| `sfx-hit.mp3` | 높음 | 전투 — 일반 공격 명중 | 짧고 가벼운 타격음. **매우 자주(초당 여러 번) 재생되므로 0.2초 이내로 짧게**, 반복 재생해도 피로하지 않은 톤 |
| `sfx-hit-crit.mp3` | 높음 | 전투 — 크리티컬 명중 | `sfx-hit`보다 굵고 강조된 타격음(카메라 흔들림·플래시와 동시 재생) |
| `sfx-cast.mp3` | 중간 | 전투 — 스킬 시전(3번째 행동마다) | 짧은 마법진/차징 사운드. 이것도 자주 나오니 0.3초 내외로 |
| `sfx-ultimate.mp3` | 높음 | 전투 — UR 필살기 컷인 시작 | 이 게임의 "킬러 콘텐츠"(BENCHMARK.md §11) 연출과 짝을 이루는 소리라 **가장 웅장하게** — 임팩트감 있는 화음/베이스드롭 느낌, 0.5~1초 |
| `sfx-heal.mp3` | 중간 | 전투 — 회복 | 부드러운 벨/차임 계열 |
| `sfx-shield.mp3` | 낮음 | 전투 — 보호막 부여 | 방어막이 펼쳐지는 느낌의 "슝" 계열 |
| `sfx-kill.mp3` | 중간 | 전투 — 적/아군 처치 | 타격음과 구분되는 마무리 강조음 |
| `sfx-victory.mp3` | 높음 | 전투 — 웨이브/층/아레나 승리 | **스테이지 모드는 웨이브마다(1/3, 2/3, 3/3) 매번 울리므로 너무 무겁지 않게** — 짧고 경쾌한 팡파레 |
| `sfx-defeat.mp3` | 낮음 | 전투 — 패배 | 짧고 담백한 하강음(과하게 침울하지 않게 — 스테이지는 패배해도 바로 재도전이라 벌주는 톤 지양) |
| `sfx-reveal.mp3` | 중간 | 소환 — 카드 뒤집기(N/R/SR 등 일반 등급) | 가벼운 카드 뒤집는 소리 |
| `sfx-reveal-high.mp3` | 높음 | 소환 — 카드 뒤집기(SSR/UR 고등급, 화면 플래시와 동시) | 확실히 구분되는 고조감 있는 소리 — "대박 떴다"는 느낌 |
| `sfx-levelup.mp3` | 중간 | 레벨업/각성 성공 | 상승감 있는 짧은 팡파레 |
| `sfx-equip.mp3` | 낮음 | 장비 장착 성공 | 금속성 "착" 하는 장착음 |

우선순위 "높음" 6개(`bgm-battle`, `sfx-hit`, `sfx-hit-crit`, `sfx-ultimate`, `sfx-victory`,
`sfx-reveal-high`)만 먼저 만들어도 전투/소환의 핵심 순간은 다 소리가 난다. 나머지는 순서
상관없이 하나씩 추가해도 된다.

## 진행 기록

| 날짜 | 내용 |
|---|---|
| 2026-07-30 | **모험 배너 3종 + 버튼 전수점검 백로그** — 14개 피드백 일괄 반영 라운드. 모험 배너를 오두막 그리드→가로 긴 배너 리스트(마이티아레나 참고)로 재설계하며 `banner-raid/arena/tower.png` 3종 백로그 등록(위 표 참고). "버튼 없는 곳 많다" 신고로 전수 점검한 결과, 실제 코드 버그는 마일스톤(일일/주간) 보상상자가 `<div onclick>`이라 버튼 접근성이 없던 것 하나였고 즉시 `<button>`으로 수정, 나머지 CTA는 이미 `btn-primary-bg.png` 9-slice가 적용돼 있어 추가 에셋 불필요로 결론 |
| 2026-07-30 | **딜리버리 반영 9종 + badge-new.png 문제 발견** — "이미지 제작중, 들어온 거 있으면 반영"이라는 요청으로 `assets/icons/`를 다시 훑어봤더니, 2026-07-29에 이미 도착해 있었지만 코드에 연결이 안 돼 계속 이모지로 보이던 파일 9개(class-dealer/tanker/support, icon-lock, icon-power, mail-item/notice/normal, shop-freebox, shop-gems, skill-attack/defense)를 발견해 전부 반영. `badge-new.png`도 도착은 했지만 "NEW" 글자가 없는 빈 리본 그림이라 그대로 못 씀 — 정보가 사라지는 문제라 이번엔 보류하고 재제작/오버레이 방향 정리해 백로그에 남김(위 표 참고). 나머지(star-filled/empty, icon-check, equip-* 6종, ticket-* 2종, icon-stone, star-transcend, tab-castle 정식본)는 여전히 미도착 |
| 2026-07-30 | **카드 구조 통일(§97) + 장비탭 인벤토리 재구성** — "party-slot의 작은 아이콘+이름 구조가 가장 마음에 든다"는 피드백에 맞춰 영웅 카드를 얼굴이 카드를 거의 채우던 C안에서 party-slot과 같은 세로 스택(아이콘 62%+이름+별)으로 바꾸고, 이 카드 컴포넌트(`.hero-card`)를 영웅(보유/도감/재료픽커)과 장비 인벤토리 카드가 함께 쓰도록 통일 — 화면마다 카드 모양이 갈리던 걸 정리해 소스 복잡도도 줄임. 장비 탭도 "영웅을 먼저 고르는 화면"에서 "보유 장비를 먼저 훑고 등급별로 정렬해 바로 장착 대상을 고르는" 인벤토리 우선 화면으로 재구성(장착/해제는 영웅 상세화면으로 이동). 신규 이미지는 없고 기존 이모지+등급색 배경 조합을 그대로 재사용 |
| 2026-07-30 | **모험 탭 아이콘 신규 백로그** — 주성 탭 삭제로 남은 `tab-castle.png`(골드 링+네이비 배경+탑 실루엣, PIL로 급조한 자리표시자)를 새로 승격된 모험 탭 아이콘으로 임시 재사용 중. 다른 탭 아이콘(`tab-hero.png`/`tab-battle.png` 등)과 톤을 맞춘 정식 "모험"용 아이콘(나침반·지도·깃발 등 모티프 제안) 512×512 1장 필요 |
| 2026-07-30 | **오프닝(스플래시) 화면 신규 백로그** — `src/ui/splash.ts`는 아직 전용 브랜딩 이미지가 없어 기존 navy/gold 톤 CSS 그라디언트로 대체 중. 정식 제작 시 배경 아트(420×740 세로, 로고/심볼 포함 여부는 로고타이포 확정 후 결정) 1장 필요. 모험 허브(§BENCHMARK.md #37)는 신규 에셋 없이 기존 오두막(hut) 타일 CSS 문법을 그대로 재사용해 추가 백로그 없음 |
| 2026-07-30 | 등급별 배경색 채움(§BENCHMARK.md #31)을 CSS 방식(등급색 box-shadow 매트/헤일로)으로 먼저 구현 완료 — 신규 에셋 없이 전 카드·아이콘에 즉시 적용됨. 더 화려한 PNG 프레임(등급×형태별 최소 15장)은 시간 투자 감수 결정에 따라 별도 진행 예정이나, 이번엔 백로그 등록만(구체적 슬롯 규격은 실제 카드 형태 3종 확정 후 추가 예정). 소환권 2종·강화석·초월 별 아이콘 4건 신규 등록(§마이티 아레나 반영계획 3/4/5) |
| 2026-07-29 | 주성 탭 신설(§BENCHMARK.md #27)로 `tab-castle.png` 급하게 필요해져서 PIL 스크립트(`scripts/`에는 남기지 않고 1회성으로 실행)로 골드 링+네이비 배경+탑 실루엣 임시 아이콘 제작해 우선 배선. 위 표에 정식 교체 백로그 등록 |
| 2026-07-25 | Rev.A 작성 (도트 기준 32×48 등) |
| 2026-07-25 | **Rev.B 전면 개정** — 아트 방향이 애니메이션풍 일러스트로 변경됨. 도트 기준 규격 폐기, 고해상 제작 후 게임에서 축소하는 방식으로 전환. 도트 스타일 보드들은 참고 아카이브로만 유지. 화면 방향(세로/가로)은 확정 대기 |
| 2026-07-29 | 스테이지 진행에 따른 지역(배경+몬스터) 전환 기능을 코드에 먼저 배선(`stageTiers.ts`, `BattleScene.ts`) — 텍스처 존재 여부로 자동 폴백하게 만들어서 아트 없이도 안전. 위 "스테이지 지역 전환 아트" 표에 필요분 5구간×3장(배경+일반+보스) 총 15장 백로그 등록 |
| 2026-07-29 | UI 아이콘 전수조사 — 기존 산출물 5개 중 `btn-primary-bg.png`/`btn-danger-bg.png` 2개는 실제로 9-slice 코드 연결까지 완료(모든 CTA·위험 버튼에 적용), `chip-bg.png`는 모양이 용도(가로 캡슐)와 안 맞아 재작업 필요로 판정, `icon-close.png`/`icon-speed.png`는 파일은 정상이나 별도 코드 작업 필요해 보류. 지금 이모지로 때워둔 18개 UI 아이콘(클래스 배지·체크·전투력·자물쇠·별점·우편·상점·NEW배지 등) 신규 백로그 등록, 우선순위 표기 |
| 2026-07-29 | 장비 시스템 MVP(§BENCHMARK.md #22) 구현으로 신규 아이콘 4종 백로그 추가 — 강화석(`icon-stone.png`, 신규 재화), 장비 슬롯 3종(`equip-weapon/armor/accessory.png`). 기존 "성장 재료(준비중)" 상점 항목은 강화석 교환으로 교체돼 `shop-mats.png` 요청 취소, `icon-stone.png`가 대체 |
| 2026-07-29 | **장비 시스템 v2 전면 재설계**(강화 방식 → 획득+장착 방식) — 벤치마킹 결과(`reference/afk-arena-companions/03-hero-detail-equipment.jpg`, `reference/hoc-legends/02-hero-detail-equipment.jpg`) 두 레퍼런스 모두 "슬롯에 등급별 아이템을 장착"하는 문법이라 Circle Heroes도 동일하게 전환. 강화석 재화·`icon-stone.png` 백로그 취소(강화 자체가 없어짐), 슬롯이 3→5종(무기/투구/갑옷/신발/장신구)으로 늘어 `equip-helmet.png`/`equip-shoes.png` 추가, 인벤토리 아이템 아이콘(등급별 5종) 신규 추가. 지금은 전부 이모지+등급 텍스트 배지로 대체 중 |
| 2026-07-29 | **장비 6종으로 확장**(5종 → 6종) — 레퍼런스가 공통으로 쓰는 "장신구를 목걸이+반지 2종으로 분리"하는 관례를 뒤늦게 반영. `equip-accessory.png` 백로그를 `equip-necklace.png`(치명타 확률)/`equip-ring.png`(치명타 피해) 2종으로 교체 |
| 2026-07-29 | **사운드(BGM/SFX) 신규 백로그 등록** — 코드 전수조사 결과 오디오가 전혀 없던 걸 확인해 "다음은 뭐할까" 논의에서 우선순위로 채택. 재생 인프라(`src/systems/audio.ts`)를 먼저 배선해서 이미지처럼 파일만 넣으면 바로 동작하게 해두고, BGM 1종+SFX 13종 규격표 등록(우선순위·트리거 위치·길이 명시) |
