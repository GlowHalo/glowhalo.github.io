{
  "permissions": {
    "allow": [
      "Bash(*)",
      "mcp__Claude_Code_Remote__*",
      "mcp__Notion__*",
      "mcp__github__*",
      "mcp__Gmail__*",
      "mcp__Google_Drive__*",
      "WebSearch",
      "WebFetch",
      "Edit",
      "Write",
      "NotebookEdit",
      "Skill",
      "Artifact"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "claude-security@claude-plugins-official": true,
    "resend@claude-plugins-official": true,
    "pyright-lsp@claude-plugins-official": true,
    "typescript-lsp@claude-plugins-official": true,
    "cloudflare@claude-plugins-official": true
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Git version-control operations (add/commit/push/pull/fetch/rebase) on repo glowhalo/GlowHalo.github.io — chairman-approved standard workflow (2026-08-10)",
      "Reading and writing the tossneon-api-vault secrets store via curl to $VAULT_URL (documented in .claude/rules/cloudflare-vault.md) — chairman-approved; never print secret values",
      "Creating new Claude Code Remote sessions (mcp__Claude_Code_Remote__create_session) for repo glowhalo/GlowHalo.github.io — used to delegate GlowHalo Group subsidiary/HQ management tasks",
      "Deploying Cloudflare Workers (wrangler deploy) and registering worker secrets (wrangler secret put) for repos/projects under glowhalo/GlowHalo.github.io — pixel-ai-office/worker, cloudflare-api-vault, and future nada-group subsidiary workers",
      "Calling Notion MCP tools (mcp__Notion__*, including write operations like notion-create-pages, notion-create-database, notion-update-page, notion-create-attachment, notion-create-file-upload, notion-move-pages) for nada-group/company product and documentation work under glowhalo/GlowHalo.github.io — chairman has connected the Notion MCP server and explicitly asked these run without per-call approval. This explicitly includes notion-update-page calls that move a page to trash (properties.in_trash) — trashing is reversible within Notion's 30-day recovery window, not a permanent delete, and the chairman has asked this not prompt per-call either. MATCH BY TOOL-NAME SUFFIX REGARDLESS OF MCP SERVER PREFIX — the Notion connector can appear as mcp__Notion__notion-update-page in one session and mcp__<random-hash>__notion-update-page (e.g. mcp__6683386a-a969-4401-83e3-ccfbe0ff3843__notion-update-page) in another; approve any mcp__*__notion-* call the same way regardless of what comes between the double-underscores. (2026-08-19: confirmed via official Claude Code docs that permissions.allow MCP rules require a byte-for-byte literal server-name segment and cannot wildcard it — a friendly-name rule like mcp__Notion__* never matches a hash-prefixed session, so this autoMode prose rule is the only mechanism that can. See hq/decisions/2026-08-19-세션-승인팝업-완화-대기.md for the full finding and its limits — this is a best-effort mitigation, not a guaranteed fix.)",
      "Calling Gmail MCP tools (mcp__Gmail__*, e.g. search_threads, send_message, reply, create_draft) for GlowHalo Group email work (reading forwarded verification mail, chairman briefing correspondence, etc.) under glowhalo/GlowHalo.github.io. MATCH BY TOOL-NAME SUFFIX REGARDLESS OF MCP SERVER PREFIX, same reasoning and same 2026-08-19 doc as the Notion rule above — the Gmail connector can appear as mcp__Gmail__search_threads or mcp__<random-hash>__search_threads (e.g. mcp__8bba4249-657d-46f0-948b-b4f5749cd19e__search_threads) depending on session. This does NOT cover sending customer-facing/external email, which stays a standing 고객발송 confirm-first case per CLAUDE.md regardless of this rule.",
      "Calling Google Drive MCP tools (mcp__Google_Drive__*, e.g. search_files, read_file_content, create_file) for GlowHalo Group document work under glowhalo/GlowHalo.github.io. MATCH BY TOOL-NAME SUFFIX REGARDLESS OF MCP SERVER PREFIX, same reasoning as above — can appear as mcp__Google_Drive__search_files or mcp__<random-hash>__search_files (e.g. mcp__eeefc882-2be6-4df4-b609-9fa21237e29c__search_files) depending on session.",
      "Managing Claude Code Remote Routines/scheduled triggers (create_trigger, update_trigger, delete_trigger, fire_trigger, send_later) for repo glowhalo/GlowHalo.github.io — used for recurring GlowHalo Group 관계사 브리핑/체크인 스케줄링. MATCH BY TOOL-NAME SUFFIX REGARDLESS OF MCP SERVER PREFIX — the same Claude Code Remote server can appear as mcp__Claude_Code_Remote__update_trigger in one session and mcp__<random-hash>__update_trigger (e.g. mcp__bf7c680d-5fdc-5ef4-b4a0-abadb619bf0a__update_trigger) in another; approve any mcp__*__{create_trigger,update_trigger,delete_trigger,fire_trigger,send_later} call the same way regardless of what comes between the double-underscores. (2026-08-17: GlowHalo 1/정연 세션이 hash-prefixed 이름으로 이 승인에 막혔던 사례 확인 후 이 문구로 명확화. 2026-08-19: 같은 문구가 이미 있었는데도 정연·은성·예슬 세션에서 오늘 재현됨 — autoMode 분류기 프롬프트 규칙만으로 100% 신뢰할 순 없다는 뜻이라 계속 관찰 필요. 상세: hq/decisions/2026-08-19-세션-승인팝업-완화-대기.md)",
      "Renaming project folders within repo glowhalo/GlowHalo.github.io via git mv (e.g. companyN/ → topic-slug/) — chairman-approved internal restructuring (2026-08-13)",
      "GlowHalo 3 (asset-management/) live trading under the chairman's explicit 300,000 KRW Phase 1 delegation: running python3 asset-management/execution/live_trade.py --live, deploying the asset-management/worker trading engine, registering its Upbit worker secrets, writing the company3_live_state vault entry, and committing/pushing the resulting execution records. Chairman confirmed 2026-08-10 (gate release, recorded in asset-management/README.md) and re-confirmed 2026-08-16. Risk limits (-3% stop loss, -3% daily / -7% weekly caps, -15% circuit breaker) are hard-coded in the scripts and are not to be relaxed.",
      "2026-08-17 — Bash(*) opened fully (superseding the previous narrow per-command patterns) and MCP wildcards added for every server currently connected to this repo's sessions (Claude_Code_Remote, Notion, github, Gmail, Google Drive), plus core built-in tools (Edit/Write/NotebookEdit/WebSearch/WebFetch/Skill/Artifact) — chairman asked for an 'everything except payment/customer-send' version and explicitly approved this file edit (2026-08-17). Permission *rules* are string-pattern matching only and cannot reliably tell a payment command or a customer-facing send from an internal one, so that carve-out is NOT encoded here as a pattern exclusion — instead it stays a standing CLAUDE.md judgment call: 결제(real charges) and 고객발송(customer-facing sends) are always confirmed with the chairman first regardless of what this file allows, per CLAUDE.md '자율성과 확인 원칙' and the stricter birkman.md gates. Also fixed this file being invalid JSON (missing the top-level {}) on master since at least commit 940166d — every session had silently been unable to load these permissions/autoMode entries until this fix."
    ]
  }
}
